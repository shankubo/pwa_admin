// Point d'extension pour les types d'un plugin externe privé (ex: pwa-admin-plugin-imanote).
// Vide par défaut — un installeur de plugin réécrit ce fichier pour ré-exporter
// ses propres types. Reste versionné avec ce contenu vide pour qu'un
// `git checkout -f origin/master` sur pwa_admin ne casse jamais le build.
export {};
