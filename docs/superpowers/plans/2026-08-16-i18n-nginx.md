# Multi-langue — Écran Nginx Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Traduire en FR/EN/TA l'écran `Nginx.tsx` (le plus gros écran du projet, 1309 lignes), complétant le lot "management" du spec i18n après Pm2/Security/OsSystem/NetworkSecurity/Sites/Docker.

**Architecture:** Migration vers `useTranslation("nginx")`, un seul namespace pour tout le fichier (cohérent avec le reste du projet — un namespace par fichier, pas par composant). Le fichier est scindé en 2 tâches pour rester gérable : Task 1 couvre l'écran principal (liste des vhosts, statut Nginx, sauvegarde de config, panneau de détail d'un vhost) ; Task 2 couvre le dialogue d'édition guidée (`GuidedConfigEditorDialog` et ses sous-composants), qui forme un bloc dense et autonome de configuration TLS/headers/gzip/etc. Les deux tâches partagent le même namespace `docker.json`-style `nginx.json`, construit progressivement (Task 1 crée les 3 fichiers avec les clés de la première moitié, Task 2 les complète en ajoutant ses propres clés sans toucher aux clés existantes).

**Tech Stack:** react-i18next (déjà installé et opérationnel), TypeScript, Vite.

**Spec:** [docs/superpowers/specs/2026-08-15-i18n-frontend-design.md](../specs/2026-08-15-i18n-frontend-design.md)

## Global Constraints

- Langues supportées : `fr` (défaut/fallback), `en`, `ta`. Namespace unique `nginx` pour tout l'écran, sous `apps/web/public/locales/<lng>/nginx.json`.
- Le namespace `nginx` doit être ajouté à la liste `ns` de `apps/web/src/lib/i18n.ts` — lire l'état actuel du fichier avant de modifier (ajout dans Task 1 seulement, Task 2 ne touche pas à `ns`).
- `formatBytes` est importé depuis `./Docker` (déjà traduit, signature inchangée) — ne pas y toucher.
- Les chaînes interpolées utilisent l'interpolation i18next, jamais de concaténation.
- Les valeurs techniques (noms de vhost, chemins de fichiers, ports, statuts HTTP bruts, directives nginx comme `ssl_protocols`/`ssl_ciphers`, placeholders d'exemple type `TLSv1.2 TLSv1.3` ou `/etc/letsencrypt/...`) ne sont PAS traduites — mais les placeholders restent dans le HTML natif `placeholder="..."`, donc doivent quand même passer par `t()` si le texte du placeholder est explicatif en langue naturelle (ex: `"Coller un certificat"` bouton) vs. rester tel quel si c'est un exemple de syntaxe technique (ex: `placeholder="20m"`, `placeholder="TLSv1.2 TLSv1.3"`, `placeholder="/etc/letsencrypt/live/exemple.fr/fullchain.pem"` — ces exemples restent identiques dans les 3 langues car ce sont des exemples de syntaxe nginx/chemin système, pas du texte d'interface).
- Les labels de checkbox d'en-têtes de sécurité (`X-Frame-Options`, `Content-Security-Policy`, etc.) restent des noms de directives HTTP standard — NE PAS traduire ces libellés eux-mêmes (identiques dans les 3 langues), mais leurs descriptions explicatives (le texte en dessous) SONT traduites.
- Après chaque tâche : `npx tsc --noEmit -p apps/web/tsconfig.json` (depuis la racine du repo) doit passer sans erreur. Note : ce repo a un fichier privé non suivi `apps/web/src/routes/ImaNote.tsx` causant des erreurs TS2305 préexistantes et sans rapport — déplacer temporairement ce fichier hors du répertoire avant `tsc` puis le remettre immédiatement après si besoin d'un signal propre.
- Aucun changement backend — hors périmètre.
- Tout le contenu traité dans ce plan est du texte d'interface original de cette application (labels de boutons, descriptions de fonctionnalités, messages de statut) — pas de contenu tiers.

---

## Task 1: Traduire l'écran principal Nginx (statut, liste des vhosts, panneau de détail)

**Files:**
- Create: `apps/web/public/locales/fr/nginx.json`
- Create: `apps/web/public/locales/en/nginx.json`
- Create: `apps/web/public/locales/ta/nginx.json`
- Modify: `apps/web/src/routes/Nginx.tsx` (lignes 1-601 du fichier actuel : `Nginx`, `SiteLink`, `certBadgeClass`, `NginxConfigBackupCard`, `VhostDetailPanel` — le reste du fichier, `GuidedConfigEditorDialog` et sous-composants, n'est PAS touché par cette tâche, Task 2 s'en charge)
- Modify: `apps/web/src/lib/i18n.ts`

**Interfaces:**
- Consumes: `useTranslation` de `react-i18next`, `formatBytes` de `./Docker` (inchangé).
- Produces: les 3 fichiers `nginx.json` créés ici seront complétés (pas remplacés) par Task 2, qui y ajoute ses propres clés de haut niveau sans toucher aux clés créées ici. Task 2 doit lire l'état réel de ces 3 fichiers avant d'ajouter ses clés.

État actuel des composants concernés : voir le contenu du fichier lu en entier ci-dessus, lignes 1 à 601. Chaînes à extraire :

**`Nginx`** (l.47-249) : "État Nginx" (l.98), "Actif"/"Inactif" (l.107), "Test config : OK"/"Échec" (l.116), texte connexions actives/req-s (l.119-122, interpolé), "Chargement…" (l.126), boutons "Recharger"/"Redémarrer" (l.131, 136), titre/description confirmation redémarrage (l.139-140), "Vhosts" (l.151, titre de section), bouton "Nouveau site" (l.156), "Chargement…"/"Aucun vhost." (l.163-164), texte "(sans server_name)" (l.182), "Ports {{ports}}" avec proxy optionnel (l.186-187, interpolé), "introuvable" (l.194), badge "maintenance" (l.203), "activé"/"désactivé" (l.212), boutons "Désactiver"/"Activer" (l.227, 237), titre/description confirmation désactivation (l.230-231, interpolé).

**`NginxConfigBackupCard`** (l.274-328) : titre "Sauvegarde de la configuration Nginx" (l.305), description (l.307-308), bouton "Sauvegarde en cours…"/"Sauvegarder maintenant" (l.312), label "Inclure Google Drive" (l.316), résultat de sauvegarde (l.320-324, deux variantes interpolées : succès avec taille+Drive optionnel, échec avec message d'erreur).

**`VhostDetailPanel`** (l.330-601) : "Certificat" (l.430), badge certificat 3 variantes (l.432-436, une interpolée avec jours restants), "Accessibilité" (l.444), badge accessible/inaccessible (l.454, interpolé avec code HTTP), bouton "Test…"/"Retester" (l.470), "Test en cours…" (l.474), "Trafic (7 derniers jours)" (l.481), texte visiteurs uniques avec pluriel (l.485-487, interpolé — attention au pluriel français "visiteur(s) unique(s)"), "Pas de données de log disponibles." (l.489), "Erreurs récentes (24h)" (l.505), "Aucune erreur récente." (l.509), "{{count}} erreur(s)" (l.512, pluriel), "Chargement…" (l.523), "Configuration brute (éditeur simple — CodeMirror différé)" (l.529), bouton "Enregistrement…"/"Enregistrer" (l.540), bouton "Éditeur guidé" (l.548), "Historique" (l.559), bouton "Restaurer" (l.568), titre/description confirmation restauration (l.571-572), onglets "Accès"/"Erreurs" (l.593).

- [ ] **Step 1: Créer les fichiers `nginx.json` (première moitié — Task 2 les complètera)**

`apps/web/public/locales/fr/nginx.json`:
```json
{
  "status": {
    "title": "État Nginx",
    "active": "Actif",
    "inactive": "Inactif",
    "configTest": "Test config : {{result}}",
    "configTestOk": "OK",
    "configTestFailed": "Échec",
    "connectionsInfo": "{{count}} connexions actives · {{rps}} req/s",
    "loading": "Chargement…",
    "reload": "Recharger",
    "restart": "Redémarrer",
    "restartConfirm": {
      "title": "Redémarrer Nginx ?",
      "description": "Cela peut interrompre brièvement tous les sites."
    }
  },
  "backup": {
    "title": "Sauvegarde de la configuration Nginx",
    "description": "Archive complète de sites-available et nginx.conf (local, et Google Drive si activé).",
    "running": "Sauvegarde en cours…",
    "runNow": "Sauvegarder maintenant",
    "includeDrive": "Inclure Google Drive",
    "successWithSize": "OK · {{size}}",
    "successDriveSuffix": " · envoyé sur Drive",
    "failed": "Échec : {{error}}"
  },
  "vhosts": {
    "sectionTitle": "Vhosts",
    "newSite": "Nouveau site",
    "loading": "Chargement…",
    "empty": "Aucun vhost.",
    "noServerName": "(sans server_name)",
    "ports": "Ports {{ports}}",
    "proxySuffix": " · proxy → {{target}}",
    "rootNotFound": "introuvable",
    "maintenanceBadge": "maintenance",
    "enabled": "activé",
    "disabled": "désactivé",
    "disable": "Désactiver",
    "enable": "Activer",
    "disableConfirm": {
      "title": "Désactiver {{name}} ?",
      "description": "Le site ne sera plus servi tant qu'il n'est pas réactivé."
    }
  },
  "detail": {
    "certificate": "Certificat",
    "certExpires": "expire dans {{days}}j",
    "certPresent": "présent",
    "certAbsent": "absent",
    "accessibility": "Accessibilité",
    "accessible": "accessible · HTTP {{statusCode}}",
    "inaccessible": "inaccessible",
    "testing": "Test…",
    "retest": "Retester",
    "testInProgress": "Test en cours…",
    "traffic": "Trafic (7 derniers jours)",
    "uniqueVisitors_one": "{{count}} visiteur unique · {{requests}} requêtes",
    "uniqueVisitors_other": "{{count}} visiteurs uniques · {{requests}} requêtes",
    "noLogData": "Pas de données de log disponibles.",
    "recentErrors": "Erreurs récentes (24h)",
    "noRecentErrors": "Aucune erreur récente.",
    "errorCount_one": "{{count}} erreur",
    "errorCount_other": "{{count}} erreurs",
    "rawConfigTitle": "Configuration brute (éditeur simple — CodeMirror différé)",
    "saving": "Enregistrement…",
    "save": "Enregistrer",
    "guidedEditor": "Éditeur guidé",
    "history": "Historique",
    "restore": "Restaurer",
    "restoreConfirm": {
      "title": "Restaurer cette version ?",
      "description": "La configuration actuelle sera remplacée."
    },
    "tabAccess": "Accès",
    "tabErrors": "Erreurs"
  }
}
```

`apps/web/public/locales/en/nginx.json`:
```json
{
  "status": {
    "title": "Nginx status",
    "active": "Active",
    "inactive": "Inactive",
    "configTest": "Config test: {{result}}",
    "configTestOk": "OK",
    "configTestFailed": "Failed",
    "connectionsInfo": "{{count}} active connections · {{rps}} req/s",
    "loading": "Loading…",
    "reload": "Reload",
    "restart": "Restart",
    "restartConfirm": {
      "title": "Restart Nginx?",
      "description": "This may briefly interrupt all sites."
    }
  },
  "backup": {
    "title": "Nginx configuration backup",
    "description": "Full archive of sites-available and nginx.conf (local, and Google Drive if enabled).",
    "running": "Backup in progress…",
    "runNow": "Back up now",
    "includeDrive": "Include Google Drive",
    "successWithSize": "OK · {{size}}",
    "successDriveSuffix": " · sent to Drive",
    "failed": "Failed: {{error}}"
  },
  "vhosts": {
    "sectionTitle": "Vhosts",
    "newSite": "New site",
    "loading": "Loading…",
    "empty": "No vhost.",
    "noServerName": "(no server_name)",
    "ports": "Ports {{ports}}",
    "proxySuffix": " · proxy → {{target}}",
    "rootNotFound": "not found",
    "maintenanceBadge": "maintenance",
    "enabled": "enabled",
    "disabled": "disabled",
    "disable": "Disable",
    "enable": "Enable",
    "disableConfirm": {
      "title": "Disable {{name}}?",
      "description": "The site will no longer be served until re-enabled."
    }
  },
  "detail": {
    "certificate": "Certificate",
    "certExpires": "expires in {{days}}d",
    "certPresent": "present",
    "certAbsent": "absent",
    "accessibility": "Accessibility",
    "accessible": "reachable · HTTP {{statusCode}}",
    "inaccessible": "unreachable",
    "testing": "Testing…",
    "retest": "Retest",
    "testInProgress": "Testing…",
    "traffic": "Traffic (last 7 days)",
    "uniqueVisitors_one": "{{count}} unique visitor · {{requests}} requests",
    "uniqueVisitors_other": "{{count}} unique visitors · {{requests}} requests",
    "noLogData": "No log data available.",
    "recentErrors": "Recent errors (24h)",
    "noRecentErrors": "No recent errors.",
    "errorCount_one": "{{count}} error",
    "errorCount_other": "{{count}} errors",
    "rawConfigTitle": "Raw configuration (simple editor — CodeMirror deferred)",
    "saving": "Saving…",
    "save": "Save",
    "guidedEditor": "Guided editor",
    "history": "History",
    "restore": "Restore",
    "restoreConfirm": {
      "title": "Restore this version?",
      "description": "The current configuration will be replaced."
    },
    "tabAccess": "Access",
    "tabErrors": "Errors"
  }
}
```

`apps/web/public/locales/ta/nginx.json`:
```json
{
  "status": {
    "title": "Nginx நிலை",
    "active": "செயலில்",
    "inactive": "செயலற்றது",
    "configTest": "கட்டமைப்பு சோதனை: {{result}}",
    "configTestOk": "OK",
    "configTestFailed": "தோல்வி",
    "connectionsInfo": "{{count}} செயலில் உள்ள இணைப்புகள் · {{rps}} req/s",
    "loading": "ஏற்றுகிறது…",
    "reload": "மீண்டும் ஏற்று",
    "restart": "மறுதொடக்கு",
    "restartConfirm": {
      "title": "Nginx ஐ மறுதொடக்கம் செய்யவா?",
      "description": "இது அனைத்து தளங்களையும் சிறிது நேரம் தடைபடச் செய்யலாம்."
    }
  },
  "backup": {
    "title": "Nginx கட்டமைப்பு காப்புப்பிரதி",
    "description": "sites-available மற்றும் nginx.conf இன் முழுமையான காப்பகம் (உள்ளூர், மற்றும் இயக்கப்பட்டிருந்தால் Google Drive).",
    "running": "காப்புப்பிரதி நடந்துகொண்டிருக்கிறது…",
    "runNow": "இப்போது காப்புப்பிரதி எடு",
    "includeDrive": "Google Drive ஐ சேர்",
    "successWithSize": "OK · {{size}}",
    "successDriveSuffix": " · Drive க்கு அனுப்பப்பட்டது",
    "failed": "தோல்வி: {{error}}"
  },
  "vhosts": {
    "sectionTitle": "Vhosts",
    "newSite": "புதிய தளம்",
    "loading": "ஏற்றுகிறது…",
    "empty": "vhost இல்லை.",
    "noServerName": "(server_name இல்லை)",
    "ports": "போர்ட்கள் {{ports}}",
    "proxySuffix": " · ப்ராக்ஸி → {{target}}",
    "rootNotFound": "கண்டறியப்படவில்லை",
    "maintenanceBadge": "பராமரிப்பு",
    "enabled": "இயக்கப்பட்டது",
    "disabled": "முடக்கப்பட்டது",
    "disable": "முடக்கு",
    "enable": "இயக்கு",
    "disableConfirm": {
      "title": "{{name}} ஐ முடக்கவா?",
      "description": "மீண்டும் இயக்கப்படும் வரை தளம் இனி வழங்கப்படாது."
    }
  },
  "detail": {
    "certificate": "சான்றிதழ்",
    "certExpires": "{{days}} நாட்களில் காலாவதியாகும்",
    "certPresent": "உள்ளது",
    "certAbsent": "இல்லை",
    "accessibility": "அணுகல்தன்மை",
    "accessible": "அணுகக்கூடியது · HTTP {{statusCode}}",
    "inaccessible": "அணுக முடியாதது",
    "testing": "சோதிக்கிறது…",
    "retest": "மீண்டும் சோதி",
    "testInProgress": "சோதனை நடந்துகொண்டிருக்கிறது…",
    "traffic": "போக்குவரத்து (கடந்த 7 நாட்கள்)",
    "uniqueVisitors_one": "{{count}} தனித்துவமான பார்வையாளர் · {{requests}} கோரிக்கைகள்",
    "uniqueVisitors_other": "{{count}} தனித்துவமான பார்வையாளர்கள் · {{requests}} கோரிக்கைகள்",
    "noLogData": "பதிவு தரவு எதுவும் இல்லை.",
    "recentErrors": "சமீபத்திய பிழைகள் (24 மணி நேரம்)",
    "noRecentErrors": "சமீபத்திய பிழைகள் இல்லை.",
    "errorCount_one": "{{count}} பிழை",
    "errorCount_other": "{{count}} பிழைகள்",
    "rawConfigTitle": "மூல கட்டமைப்பு (எளிய திருத்தி — CodeMirror தள்ளிவைக்கப்பட்டது)",
    "saving": "சேமிக்கிறது…",
    "save": "சேமி",
    "guidedEditor": "வழிகாட்டப்பட்ட திருத்தி",
    "history": "வரலாறு",
    "restore": "மீட்டமை",
    "restoreConfirm": {
      "title": "இந்த பதிப்பை மீட்டமைக்கவா?",
      "description": "தற்போதைய கட்டமைப்பு மாற்றப்படும்."
    },
    "tabAccess": "அணுகல்",
    "tabErrors": "பிழைகள்"
  }
}
```

- [ ] **Step 2: Traduire les composants ciblés dans `Nginx.tsx` (lignes 1-601)**

Le fichier complet fait 1310 lignes ; cette tâche modifie uniquement les composants `Nginx`, `SiteLink`, `certBadgeClass`, `NginxConfigBackupCard`, `VhostDetailPanel` (jusqu'à la ligne 601 de l'original). Le reste du fichier (`DEFAULT_GUIDED_MODEL`, `GuidedConfigEditorDialogProps`, `HeaderCheckbox`, `CertImportPanel`, `GuidedConfigEditorDialog`, lignes 603-1310) reste identique à l'original et NE DOIT PAS être modifié dans cette tâche — Task 2 s'en charge séparément.

Remplacer les lignes 1 à 601 du fichier `apps/web/src/routes/Nginx.tsx` (tout ce qui précède `const DEFAULT_GUIDED_MODEL`) par :
```tsx
import { useEffect, useState } from "react";
import * as Dialog from "@radix-ui/react-dialog";
import type {
  WebServerStatus,
  VhostSummary,
  VhostDetail,
  ConfigSnapshot,
  CertStatus,
  VhostAccessibility,
  VhostErrorSummary,
  ConfigBackupRun,
  GuidedFormModel,
  TopPageEntry,
  VisitorStats,
} from "@pwa-admin/shared";
import { useTranslation } from "react-i18next";
import { apiFetch, apiJson } from "@/lib/api";
import { useWsChannel } from "@/lib/ws";
import { Card, CardTitle } from "@/components/ui/Card";
import { Button } from "@/components/ui/Button";
import { ConfirmDialog } from "@/components/ui/ConfirmDialog";
import { LiveLogPanel } from "@/components/LiveLogPanel";
import { formatBytes } from "./Docker";
import {
  CheckCircle2,
  XCircle,
  ChevronDown,
  ChevronUp,
  RotateCw,
  History,
  Globe,
  AlertTriangle,
  Users,
  DatabaseBackup,
  FolderX,
  Wand2,
  X,
  Plus,
  Upload,
} from "lucide-react";

function vhostCardClass(v: VhostSummary): string {
  if (!v.enabled) return "border-muted-foreground/30 bg-muted/30";
  if (v.maintenanceMode) return "border-warning/50 bg-warning/5";
  return "border-primary/40 bg-primary/5";
}

export function Nginx() {
  const { t } = useTranslation("nginx");
  const [status, setStatus] = useState<WebServerStatus | null>(null);
  const [vhosts, setVhosts] = useState<VhostSummary[] | null>(null);
  const [expanded, setExpanded] = useState<string | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [busy, setBusy] = useState(false);

  async function load() {
    try {
      const [s, v] = await Promise.all([
        apiJson<WebServerStatus>("/nginx/status"),
        apiJson<VhostSummary[]>("/nginx/vhosts"),
      ]);
      setStatus(s);
      setVhosts(v);
    } catch (err) {
      setError((err as Error).message);
    }
  }

  useEffect(() => {
    load();
  }, []);

  async function reload() {
    setBusy(true);
    try {
      await apiJson("/nginx/reload", { method: "POST" });
      await load();
    } catch (err) {
      setError((err as Error).message);
    } finally {
      setBusy(false);
    }
  }

  async function restart() {
    await apiJson("/nginx/restart", { method: "POST", body: JSON.stringify({ confirm: true }) });
    await load();
  }

  async function toggleVhost(name: string, enabled: boolean) {
    await apiJson(`/nginx/vhosts/${name}/${enabled ? "disable" : "enable"}`, { method: "POST" });
    await load();
  }

  return (
    <div className="flex flex-col gap-4">
      {error && <Card className="text-sm text-destructive">{error}</Card>}

      <Card>
        <CardTitle>{t("status.title")}</CardTitle>
        {status ? (
          <div className="flex flex-col gap-1 text-sm">
            <div className="flex items-center gap-2">
              {status.active ? (
                <CheckCircle2 className="h-4 w-4 text-primary" />
              ) : (
                <XCircle className="h-4 w-4 text-destructive" />
              )}
              <span>{status.active ? t("status.active") : t("status.inactive")}</span>
              {status.version && <span className="text-muted-foreground">· {status.version}</span>}
            </div>
            <div className="flex items-center gap-2">
              {status.configTestOk ? (
                <CheckCircle2 className="h-4 w-4 text-primary" />
              ) : (
                <XCircle className="h-4 w-4 text-destructive" />
              )}
              <span>
                {t("status.configTest", {
                  result: status.configTestOk ? t("status.configTestOk") : t("status.configTestFailed"),
                })}
              </span>
            </div>
            {status.stubStatus && (
              <p className="text-xs text-muted-foreground">
                {t("status.connectionsInfo", {
                  count: status.stubStatus.activeConnections,
                  rps: status.stubStatus.requestsPerSecond.toFixed(1),
                })}
              </p>
            )}
          </div>
        ) : (
          <p className="text-sm text-muted-foreground">{t("status.loading")}</p>
        )}

        <div className="mt-3 flex gap-2">
          <Button size="sm" variant="outline" disabled={busy} onClick={reload}>
            <RotateCw className="h-3.5 w-3.5" /> {t("status.reload")}
          </Button>
          <ConfirmDialog
            trigger={
              <Button size="sm" variant="destructive">
                {t("status.restart")}
              </Button>
            }
            title={t("status.restartConfirm.title")}
            description={t("status.restartConfirm.description")}
            confirmLabel={t("status.restart")}
            onConfirm={restart}
          />
        </div>
      </Card>

      <NginxConfigBackupCard />

      <div>
        <div className="mb-2 flex items-center justify-between">
          <h2 className="text-sm font-semibold text-muted-foreground">{t("vhosts.sectionTitle")}</h2>
          <GuidedConfigEditorDialog
            mode="create"
            trigger={
              <Button size="sm" variant="outline">
                <Plus className="h-3.5 w-3.5" /> {t("vhosts.newSite")}
              </Button>
            }
            onCreated={load}
          />
        </div>
        <div className="flex flex-col gap-3">
          {!vhosts && <Card className="text-sm text-muted-foreground">{t("vhosts.loading")}</Card>}
          {vhosts?.length === 0 && <Card className="text-sm text-muted-foreground">{t("vhosts.empty")}</Card>}
          {vhosts?.map((v) => (
            <Card key={v.name} className={vhostCardClass(v)}>
              <div
                className="flex cursor-pointer items-start justify-between gap-2"
                onClick={() => setExpanded(expanded === v.name ? null : v.name)}
              >
                <div className="min-w-0 flex-1">
                  <p className="truncate font-medium">{v.name}</p>
                  <p className="truncate text-xs text-muted-foreground">
                    {v.serverNames.length > 0 ? (
                      v.serverNames.map((sn, i) => (
                        <span key={sn}>
                          {i > 0 && ", "}
                          <SiteLink host={sn} usesTls={v.listenPorts.includes(443)} />
                        </span>
                      ))
                    ) : (
                      t("vhosts.noServerName")
                    )}
                  </p>
                  <p className="text-xs text-muted-foreground">
                    {t("vhosts.ports", { ports: v.listenPorts.join(", ") })}
                    {v.proxyPassTarget ? t("vhosts.proxySuffix", { target: v.proxyPassTarget }) : ""}
                  </p>
                  {v.root && (
                    <p className="truncate text-xs text-muted-foreground">
                      {v.root}
                      {v.documentRootExists === false && (
                        <span className="ml-1 inline-flex items-center gap-0.5 text-destructive">
                          <FolderX className="h-3 w-3" /> {t("vhosts.rootNotFound")}
                        </span>
                      )}
                    </p>
                  )}
                </div>
                <div className="flex shrink-0 items-center gap-2">
                  {v.enabled && v.maintenanceMode && (
                    <span className="rounded-full bg-warning/15 px-2 py-0.5 text-xs font-medium text-warning">
                      {t("vhosts.maintenanceBadge")}
                    </span>
                  )}
                  <span
                    className={
                      "rounded-full px-2 py-0.5 text-xs font-medium " +
                      (v.enabled ? "bg-primary/15 text-primary" : "bg-muted text-muted-foreground")
                    }
                  >
                    {v.enabled ? t("vhosts.enabled") : t("vhosts.disabled")}
                  </span>
                  {expanded === v.name ? (
                    <ChevronUp className="h-4 w-4 text-muted-foreground" />
                  ) : (
                    <ChevronDown className="h-4 w-4 text-muted-foreground" />
                  )}
                </div>
              </div>

              <div className="mt-3 flex flex-wrap gap-2" onClick={(e) => e.stopPropagation()}>
                {v.enabled ? (
                  <ConfirmDialog
                    trigger={
                      <Button size="sm" variant="destructive">
                        {t("vhosts.disable")}
                      </Button>
                    }
                    title={t("vhosts.disableConfirm.title", { name: v.name })}
                    description={t("vhosts.disableConfirm.description")}
                    confirmLabel={t("vhosts.disable")}
                    onConfirm={() => toggleVhost(v.name, v.enabled)}
                  />
                ) : (
                  <Button size="sm" variant="outline" onClick={() => toggleVhost(v.name, v.enabled)}>
                    {t("vhosts.enable")}
                  </Button>
                )}
              </div>

              {expanded === v.name && <VhostDetailPanel name={v.name} onChanged={load} />}
            </Card>
          ))}
        </div>
      </div>
    </div>
  );
}

function SiteLink({ host, usesTls }: { host: string; usesTls: boolean }) {
  if (host === "_" || !host) return <span>{host}</span>;
  const url = `${usesTls ? "https" : "http"}://${host}/`;
  return (
    <a
      href={url}
      target="_blank"
      rel="noopener noreferrer"
      onClick={(e) => e.stopPropagation()}
      className="text-primary underline-offset-2 hover:underline"
    >
      {host}
    </a>
  );
}

function certBadgeClass(daysRemaining: number | null) {
  if (daysRemaining == null) return "bg-muted text-muted-foreground";
  if (daysRemaining < 14) return "bg-destructive/15 text-destructive";
  if (daysRemaining < 30) return "bg-warning/15 text-warning";
  return "bg-primary/15 text-primary";
}

function NginxConfigBackupCard() {
  const { t } = useTranslation("nginx");
  const [running, setRunning] = useState(false);
  const [lastRun, setLastRun] = useState<ConfigBackupRun | null>(null);
  const [useDrive, setUseDrive] = useState(true);

  async function runBackup() {
    setRunning(true);
    try {
      const run = await apiJson<ConfigBackupRun>("/nginx/config/backup", {
        method: "POST",
        body: JSON.stringify({ targets: useDrive ? ["local", "gdrive"] : ["local"] }),
      });
      setLastRun(run);
    } catch (err) {
      setLastRun({
        runId: "",
        status: "failed",
        sizeBytes: null,
        driveFileId: null,
        error: (err as Error).message,
        startedAt: new Date().toISOString(),
        finishedAt: new Date().toISOString(),
      });
    } finally {
      setRunning(false);
    }
  }

  return (
    <Card>
      <CardTitle className="flex items-center gap-1">
        <DatabaseBackup className="h-4 w-4" /> {t("backup.title")}
      </CardTitle>
      <p className="text-xs text-muted-foreground">{t("backup.description")}</p>
      <div className="mt-3 flex items-center gap-3">
        <Button size="sm" disabled={running} onClick={runBackup}>
          {running ? t("backup.running") : t("backup.runNow")}
        </Button>
        <label className="flex items-center gap-1.5 text-xs text-muted-foreground">
          <input type="checkbox" checked={useDrive} onChange={(e) => setUseDrive(e.target.checked)} />
          {t("backup.includeDrive")}
        </label>
      </div>
      {lastRun && (
        <p className={`mt-2 text-xs ${lastRun.status === "success" ? "text-primary" : "text-destructive"}`}>
          {lastRun.status === "success"
            ? t("backup.successWithSize", { size: lastRun.sizeBytes ? formatBytes(lastRun.sizeBytes) : "" }) +
              (lastRun.driveFileId ? t("backup.successDriveSuffix") : "")
            : t("backup.failed", { error: lastRun.error })}
        </p>
      )}
    </Card>
  );
}

function VhostDetailPanel({ name, onChanged }: { name: string; onChanged: () => void }) {
  const { t } = useTranslation("nginx");
  const [detail, setDetail] = useState<VhostDetail | null>(null);
  const [cert, setCert] = useState<CertStatus | null>(null);
  const [history, setHistory] = useState<ConfigSnapshot[] | null>(null);
  const [content, setContent] = useState("");
  const [saveError, setSaveError] = useState<string | null>(null);
  const [saving, setSaving] = useState(false);
  const [logType, setLogType] = useState<"access" | "error">("error");
  const [logChunk, setLogChunk] = useState<string | null>(null);
  const [initialLog, setInitialLog] = useState<string>("");
  const [accessibility, setAccessibility] = useState<VhostAccessibility | null>(null);
  const [checkingAccess, setCheckingAccess] = useState(false);
  const [errorSummary, setErrorSummary] = useState<VhostErrorSummary | null>(null);
  const [topPages, setTopPages] = useState<TopPageEntry[] | null>(null);
  const [visitors, setVisitors] = useState<VisitorStats | null>(null);

  async function load() {
    const [d, c, h] = await Promise.all([
      apiJson<VhostDetail>(`/nginx/vhosts/${name}`),
      apiJson<CertStatus>(`/nginx/vhosts/${name}/cert`),
      apiJson<ConfigSnapshot[]>(`/nginx/vhosts/${name}/history`),
    ]);
    setDetail(d);
    setContent(d.rawConfig);
    setCert(c);
    setHistory(h);
  }

  async function checkAccess() {
    setCheckingAccess(true);
    try {
      setAccessibility(await apiJson<VhostAccessibility>(`/nginx/vhosts/${name}/accessibility`));
    } catch (err) {
      setAccessibility({
        checkedUrl: null,
        reachable: false,
        statusCode: null,
        latencyMs: null,
        error: (err as Error).message,
        checkedAt: new Date().toISOString(),
      });
    } finally {
      setCheckingAccess(false);
    }
  }

  useEffect(() => {
    load().catch(() => {});
    checkAccess().catch(() => {});
    apiJson<VhostErrorSummary>(`/nginx/vhosts/${name}/errors?window=24&limit=20`)
      .then(setErrorSummary)
      .catch(() => setErrorSummary(null));
    apiJson<TopPageEntry[]>(`/analytics/sites/${name}/top-pages?window=7`)
      .then(setTopPages)
      .catch(() => setTopPages(null));
    apiJson<VisitorStats>(`/analytics/sites/${name}/visitors?window=7`)
      .then(setVisitors)
      .catch(() => setVisitors(null));
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [name]);

  useEffect(() => {
    setLogChunk(null);
    apiFetch(`/nginx/vhosts/${name}/logs?type=${logType}&tail=200`)
      .then((res) => (res.ok ? res.text() : ""))
      .then(setInitialLog)
      .catch(() => setInitialLog(""));
  }, [name, logType]);

  useWsChannel("nginx.logs", (frame) => setLogChunk(frame.data as string), { name, type: logType });

  async function save() {
    setSaving(true);
    setSaveError(null);
    try {
      const res = await apiFetch(`/nginx/vhosts/${name}/config`, {
        method: "PUT",
        body: JSON.stringify({ content }),
      });
      if (!res.ok) {
        const body = await res.json().catch(() => ({ error: "save_failed" }));
        setSaveError(body.output ? `${body.error}: ${body.output}` : body.error ?? "save_failed");
        return;
      }
      await load();
    } finally {
      setSaving(false);
    }
  }

  async function restoreSnapshot(id: number) {
    await apiJson(`/nginx/vhosts/${name}/history/${id}/restore`, { method: "POST" });
    await load();
    onChanged();
  }

  return (
    <div className="mt-3 flex flex-col gap-4 border-t border-border pt-3">
      {cert && (
        <div>
          <p className="mb-1 text-xs font-medium text-muted-foreground">{t("detail.certificate")}</p>
          <span className={`rounded-full px-2 py-0.5 text-xs font-medium ${certBadgeClass(cert.daysRemaining)}`}>
            {cert.found
              ? cert.daysRemaining != null
                ? t("detail.certExpires", { days: cert.daysRemaining })
                : t("detail.certPresent")
              : t("detail.certAbsent")}
          </span>
          {cert.subject && <p className="mt-1 text-xs text-muted-foreground">{cert.subject}</p>}
        </div>
      )}

      <div>
        <p className="mb-1 flex items-center gap-1 text-xs font-medium text-muted-foreground">
          <Globe className="h-3.5 w-3.5" /> {t("detail.accessibility")}
        </p>
        {accessibility ? (
          <div className="flex flex-wrap items-center gap-2 text-xs">
            <span
              className={
                "rounded-full px-2 py-0.5 font-medium " +
                (accessibility.reachable ? "bg-primary/15 text-primary" : "bg-destructive/15 text-destructive")
              }
            >
              {accessibility.reachable
                ? t("detail.accessible", { statusCode: accessibility.statusCode })
                : t("detail.inaccessible")}
            </span>
            {accessibility.latencyMs != null && (
              <span className="text-muted-foreground">{accessibility.latencyMs} ms</span>
            )}
            {accessibility.checkedUrl && (
              <a
                href={accessibility.checkedUrl}
                target="_blank"
                rel="noopener noreferrer"
                className="text-primary underline-offset-2 hover:underline"
              >
                {accessibility.checkedUrl}
              </a>
            )}
            <Button size="sm" variant="outline" disabled={checkingAccess} onClick={checkAccess}>
              {checkingAccess ? t("detail.testing") : t("detail.retest")}
            </Button>
          </div>
        ) : (
          <p className="text-xs text-muted-foreground">{t("detail.testInProgress")}</p>
        )}
        {accessibility?.error && <p className="mt-1 text-xs text-destructive">{accessibility.error}</p>}
      </div>

      <div>
        <p className="mb-1 flex items-center gap-1 text-xs font-medium text-muted-foreground">
          <Users className="h-3.5 w-3.5" /> {t("detail.traffic")}
        </p>
        {visitors ? (
          <p className="text-xs text-muted-foreground">
            {t("detail.uniqueVisitors", { count: visitors.uniqueIps, requests: visitors.totalRequests })}
          </p>
        ) : (
          <p className="text-xs text-muted-foreground">{t("detail.noLogData")}</p>
        )}
        {topPages && topPages.length > 0 && (
          <div className="mt-1 flex flex-col gap-0.5">
            {topPages.slice(0, 5).map((p) => (
              <div key={p.path} className="flex items-center justify-between text-xs text-muted-foreground">
                <span className="truncate">{p.path}</span>
                <span className="shrink-0 pl-2">{p.hits}</span>
              </div>
            ))}
          </div>
        )}
      </div>

      <div>
        <p className="mb-1 flex items-center gap-1 text-xs font-medium text-muted-foreground">
          <AlertTriangle className="h-3.5 w-3.5" /> {t("detail.recentErrors")}
        </p>
        {errorSummary ? (
          errorSummary.recentCount === 0 ? (
            <p className="text-xs text-muted-foreground">{t("detail.noRecentErrors")}</p>
          ) : (
            <div className="flex flex-col gap-1">
              <p className="text-xs text-muted-foreground">{t("detail.errorCount", { count: errorSummary.recentCount })}</p>
              <div className="max-h-32 overflow-y-auto rounded-md border border-border bg-black/90 p-2 font-mono text-[10px] text-destructive">
                {errorSummary.entries.map((e, i) => (
                  <div key={i} className="truncate">
                    {e.raw}
                  </div>
                ))}
              </div>
            </div>
          )
        ) : (
          <p className="text-xs text-muted-foreground">{t("detail.loading", { defaultValue: t("status.loading") })}</p>
        )}
      </div>

      <div>
        <p className="mb-1 text-xs font-medium text-muted-foreground">{t("detail.rawConfigTitle")}</p>
        <textarea
          value={content}
          onChange={(e) => setContent(e.target.value)}
          spellCheck={false}
          className="h-56 w-full resize-y rounded-md border border-border bg-black/90 p-2 font-mono text-xs text-green-400 outline-none focus:ring-2 focus:ring-primary"
        />
        {saveError && <p className="mt-1 text-xs text-destructive">{saveError}</p>}
        <div className="mt-2 flex gap-2">
          <Button size="sm" disabled={saving} onClick={save}>
            {saving ? t("detail.saving") : t("detail.save")}
          </Button>
          <GuidedConfigEditorDialog
            mode="edit"
            vhostName={name}
            content={content}
            trigger={
              <Button size="sm" variant="outline">
                <Wand2 className="h-3.5 w-3.5" /> {t("detail.guidedEditor")}
              </Button>
            }
            onApply={(newContent) => setContent(newContent)}
          />
        </div>
      </div>

      {history && history.length > 0 && (
        <div>
          <p className="mb-1 flex items-center gap-1 text-xs font-medium text-muted-foreground">
            <History className="h-3.5 w-3.5" /> {t("detail.history")}
          </p>
          <div className="flex flex-col gap-1">
            {history.map((h) => (
              <div key={h.id} className="flex items-center justify-between rounded-md border border-border p-2 text-xs">
                <span>{new Date(h.createdAt).toLocaleString()}</span>
                <ConfirmDialog
                  trigger={
                    <Button size="sm" variant="outline">
                      {t("detail.restore")}
                    </Button>
                  }
                  title={t("detail.restoreConfirm.title")}
                  description={t("detail.restoreConfirm.description")}
                  confirmLabel={t("detail.restore")}
                  onConfirm={() => restoreSnapshot(h.id)}
                />
              </div>
            ))}
          </div>
        </div>
      )}

      <div>
        <div className="mb-1 flex gap-1">
          {(["access", "error"] as const).map((tabKey) => (
            <button
              key={tabKey}
              onClick={() => setLogType(tabKey)}
              className={
                "rounded-md px-2 py-1 text-xs font-medium " +
                (logType === tabKey ? "bg-primary text-primary-foreground" : "bg-muted text-muted-foreground")
              }
            >
              {tabKey === "access" ? t("detail.tabAccess") : t("detail.tabErrors")}
            </button>
          ))}
        </div>
        <LiveLogPanel chunk={logChunk} initialText={initialLog} />
      </div>
    </div>
  );
}

```

Note importante 1 : le bloc "Erreurs récentes" avait un fallback `"Chargement…"` (l.523 de l'original) qui n'a pas de clé dédiée dans le JSON de cette tâche — pour éviter une clé orpheline `detail.loading` inexistante, ce fallback réutilise directement `t("status.loading")` (déjà défini) via un appel simple `t("status.loading")` — **remplacer la ligne indiquée dans le code ci-dessus** :
```tsx
          <p className="text-xs text-muted-foreground">{t("detail.loading", { defaultValue: t("status.loading") })}</p>
```
par la version simplifiée :
```tsx
          <p className="text-xs text-muted-foreground">{t("status.loading")}</p>
```
(Le `defaultValue` imbriqué était une erreur de rédaction dans ce plan — utiliser directement `t("status.loading")`, pas de clé `detail.loading` à créer.)

Note importante 2 : la boucle `.map((t) => ...)` sur les onglets access/error dans le fichier original (l.584) utilisait `t` comme nom de variable de boucle, ce qui entre en collision avec la fonction de traduction `t` du hook `useTranslation`. Le code ci-dessus renomme cette variable de boucle en `tabKey` pour éviter le shadowing — c'est un changement nécessaire, pas une erreur à corriger.

Note importante 3 : `formatBytes(lastRun.sizeBytes)` dans `NginxConfigBackupCard` retourne une chaîne déjà formatée (ex: "12.3 MB") qui est ensuite interpolée dans `t("backup.successWithSize", { size: ... })` — l'interpolation ne traduit pas cette valeur, elle l'insère telle quelle, ce qui est le comportement voulu (les unités B/KB/MB/GB/TB ne sont pas traduites, cf. Global Constraints).

- [ ] **Step 3: Lire l'état actuel de `lib/i18n.ts` et ajouter le namespace `nginx`**

Lire `apps/web/src/lib/i18n.ts` pour connaître l'état exact de la ligne `ns: [...]` avant de modifier. Ajouter `"nginx"` à la fin du tableau existant.

- [ ] **Step 4: Vérifier le typecheck**

Run: `npx tsc --noEmit -p apps/web/tsconfig.json`
Expected: aucune erreur (hors bruit préexistant `ImaNote.tsx`, voir Global Constraints).

- [ ] **Step 5: Vérifier que les lignes 603+ du fichier original (GuidedConfigEditorDialog et sous-composants) sont bien intactes et inchangées**

Run: `grep -c "DEFAULT_GUIDED_MODEL\|HeaderCheckbox\|CertImportPanel\|GuidedConfigEditorDialog" apps/web/src/routes/Nginx.tsx`
Expected: le fichier contient toujours ces identifiants (au moins 4 occurrences), confirmant que cette partie du fichier n'a pas été supprimée par erreur — elle doit rester exactement comme dans la version originale, pas encore traduite (Task 2 s'en charge).

- [ ] **Step 6: Commit**

```bash
git add apps/web/public/locales/fr/nginx.json apps/web/public/locales/en/nginx.json apps/web/public/locales/ta/nginx.json apps/web/src/routes/Nginx.tsx apps/web/src/lib/i18n.ts
git commit -m "feat(web): traduire l'écran Nginx (statut, vhosts, détail) via i18next"
```

---

## Task 2: Traduire le dialogue d'édition guidée (`GuidedConfigEditorDialog` et sous-composants)

**Files:**
- Modify: `apps/web/public/locales/fr/nginx.json` (ajout de clés, ne pas retirer/modifier les clés existantes de Task 1)
- Modify: `apps/web/public/locales/en/nginx.json` (idem)
- Modify: `apps/web/public/locales/ta/nginx.json` (idem)
- Modify: `apps/web/src/routes/Nginx.tsx` (lignes 603-1310 de l'original : `DEFAULT_GUIDED_MODEL`, `GuidedConfigEditorDialogProps`, `HeaderCheckbox`, `CertImportPanel`, `GuidedConfigEditorDialog`)

**Interfaces:**
- Consumes: `useTranslation("nginx")` (même namespace que Task 1, fichiers déjà créés par Task 1 — cette tâche AJOUTE des clés de haut niveau, ne touche jamais aux clés déjà présentes sous `status`/`backup`/`vhosts`/`detail`).
- Produces: rien de consommé par d'autres tâches (dernier morceau de l'écran Nginx).

Avant de commencer, lire l'état réel de `apps/web/src/routes/Nginx.tsx` (les lignes 603+ doivent être identiques à la version originale lue en préparation de ce plan, sauf si Task 1 a laissé une trace différente — dans ce cas, se fier au fichier réel, pas à cette description) et l'état réel des 3 fichiers `nginx.json` créés par Task 1.

État des composants concernés (contenu original, lignes 603-1310) : `HeaderCheckbox` (composant réutilisable pour les checkboxes d'en-têtes de sécurité, l.650-670), `CertImportPanel` (import de certificat par collage ou upload, l.680-827), `GuidedConfigEditorDialog` (le dialogue principal, l.829-1308). Chaînes à extraire :

**`CertImportPanel`** : boutons "Coller un certificat"/"Importer des fichiers" (l.746, 749), titres "Coller le certificat et la clé"/"Importer les fichiers" (l.759), aria-label "Fermer" implicite via icône (pas de texte), labels "Certificat (PEM, ex: fullchain.pem)"/"Clé privée (PEM, ex: privkey.pem)" (l.768, 778) — placeholders `"-----BEGIN CERTIFICATE-----"`/`"-----BEGIN PRIVATE KEY-----"` restent identiques (format technique PEM standard, pas de texte à traduire), bouton "Enregistrement…"/"Enregistrer" (l.788), labels "Fichier certificat (.pem/.crt)"/"Fichier clé privée (.pem/.key)" (l.796, 805), bouton "Import en cours…"/"Importer" (l.814), message succès "Certificat enregistré et chemins renseignés ci-dessus." (l.822).

**`GuidedConfigEditorDialog`** : message d'erreur "Nom de fichier et server_name requis." (l.897), titres de dialogue "Nouveau site — configuration guidée"/"Éditeur guidé" (l.926), aria-label "Fermer" (l.928), labels "Nom du fichier (sites-available)"/"server_name"/"Port d'écoute" (l.937, 947, 957) — placeholders `"mon-site"`/`"exemple.fr www.exemple.fr"` restent identiques (exemples), label+description "Activer SSL (HTTPS)" (l.975, 977-978), labels "Chemin du certificat"/"Chemin de la clé privée" (l.983, 1012) — placeholders de chemin restent identiques, bouton "Vérifier" (l.996), messages "Vérification…"/"Fichier trouvé."/"Fichier introuvable à cet emplacement." (l.999, 1002, 1007), message avertissement nom de certificat désynchronisé (l.1026-1029, interpolé avec 2 noms), message "Renseignez le nom du fichier ci-dessus pour pouvoir importer un certificat." (l.1034-1036), label+description "Taille maximale des requêtes" (l.1043, 1044-1047), titre "En-têtes de sécurité" (l.1058) — les 7 `HeaderCheckbox` gardent leurs `label` techniques inchangés (X-Frame-Options, etc., ce sont des noms de directives HTTP standard) mais leurs `description` sont traduites (l.1062, 1068, 1074, 1080, 1086, 1092, 1098), titre+description "TLS avancé" (l.1107, 1108-1112), labels "Protocoles (ssl_protocols)"/"Suites de chiffrement (ssl_ciphers)"/"Cache de session"/"Durée de session" (l.1115, 1125, 1136, 1148) — placeholders techniques inchangés, label+description "Compression Gzip" (l.1171, 1173-1175), titre+description "Blocs de contenu prédéfinis" (l.1188, 1189-1192), les 3 blocs prédéfinis avec label+description chacun : "Bloquer les fichiers cachés..." (l.1204-1207), "Cache long pour /assets/" (l.1220-1223), "Fallback SPA..." (l.1237-1240), titre+description "Mode de service" (l.1248, 1249-1252), radios "Fichiers statiques"/"Proxy" (l.1260, 1268), boutons "Annuler" (l.1297), "..."/"Créer le site"/"Appliquer" (l.1301).

- [ ] **Step 1: Lire l'état actuel des 3 fichiers `nginx.json` et y ajouter les clés du dialogue guidé**

Lire `apps/web/public/locales/fr/nginx.json` (et les versions en/ta) pour connaître l'état exact laissé par Task 1, puis ajouter une nouvelle clé de haut niveau `"guidedEditor"` à la fin de chaque fichier (après la clé `"detail"` existante), sans toucher aux clés déjà présentes.

Ajouter dans `apps/web/public/locales/fr/nginx.json`, juste avant la accolade fermante finale du JSON (donc après la clé `"detail": { ... }` déjà présente, en ajoutant une virgule après cette clé) :
```json
  "guidedEditor": {
    "certImport": {
      "pasteButton": "Coller un certificat",
      "uploadButton": "Importer des fichiers",
      "pasteTitle": "Coller le certificat et la clé",
      "uploadTitle": "Importer les fichiers",
      "close": "Fermer",
      "certPemLabel": "Certificat (PEM, ex: fullchain.pem)",
      "keyPemLabel": "Clé privée (PEM, ex: privkey.pem)",
      "saving": "Enregistrement…",
      "save": "Enregistrer",
      "certFileLabel": "Fichier certificat (.pem/.crt)",
      "keyFileLabel": "Fichier clé privée (.pem/.key)",
      "importing": "Import en cours…",
      "import": "Importer",
      "success": "Certificat enregistré et chemins renseignés ci-dessus."
    },
    "requiredFields": "Nom de fichier et server_name requis.",
    "createTitle": "Nouveau site — configuration guidée",
    "editTitle": "Éditeur guidé",
    "close": "Fermer",
    "fileNameLabel": "Nom du fichier (sites-available)",
    "serverNameLabel": "server_name",
    "listenPortLabel": "Port d'écoute",
    "sslLabel": "Activer SSL (HTTPS)",
    "sslDescription": "Active HTTPS pour ce site en référençant un certificat et sa clé déjà présents sur le serveur.",
    "certPathLabel": "Chemin du certificat",
    "certKeyPathLabel": "Chemin de la clé privée",
    "verify": "Vérifier",
    "verifying": "Vérification…",
    "certFound": "Fichier trouvé.",
    "certMissing": "Fichier introuvable à cet emplacement.",
    "certNameMismatch": "Le certificat importé était lié au nom « {{oldName}} » — réimportez-le pour « {{newName}} » avant d'enregistrer.",
    "certNamePrompt": "Renseignez le nom du fichier ci-dessus pour pouvoir importer un certificat.",
    "maxBodySizeLabel": "Taille maximale des requêtes",
    "maxBodySizeDescription": "Limite la taille des fichiers/formulaires envoyés par les visiteurs (ex : 20m pour 20 Mo). Laisser vide pour garder la valeur par défaut de Nginx (1 Mo).",
    "securityHeadersTitle": "En-têtes de sécurité",
    "headers": {
      "frameOptions": "Empêche le site d'être affiché dans un iframe sur un autre domaine (anti-clickjacking).",
      "contentTypeOptions": "Empêche le navigateur de deviner le type d'un fichier différemment de ce que déclare le serveur.",
      "referrerPolicy": "Limite les informations envoyées aux autres sites quand un visiteur clique sur un lien sortant.",
      "hsts": "Force les navigateurs à toujours utiliser HTTPS pour ce site. À activer seulement si SSL est bien configuré.",
      "xssProtection": "Ancienne protection navigateur contre les attaques XSS réfléchies (dépréciée mais encore acceptée).",
      "permissionsPolicy": "Désactive l'accès à la caméra, au micro, à la géolocalisation, etc. pour ce site.",
      "contentSecurityPolicy": "Restreint les sources de scripts/styles autorisées (protection XSS avancée). Valeur générique de base — à adapter si le site charge des ressources externes (polices, CDN, etc.)."
    },
    "tlsAdvancedTitle": "TLS avancé",
    "tlsAdvancedDescription": "Réglages optionnels pour durcir/aligner la configuration TLS. Laisser vide pour garder les valeurs par défaut de Nginx. Les valeurs se collent généralement depuis un générateur externe (ex : Mozilla SSL Config Generator).",
    "protocolsLabel": "Protocoles (ssl_protocols)",
    "ciphersLabel": "Suites de chiffrement (ssl_ciphers)",
    "sessionCacheLabel": "Cache de session",
    "sessionTimeoutLabel": "Durée de session",
    "gzipLabel": "Compression Gzip",
    "gzipDescription": "Compresse les réponses avant envoi pour accélérer le chargement (texte, CSS, JS, JSON...).",
    "presetsTitle": "Blocs de contenu prédéfinis",
    "presetsDescription": "Modèles courants prêts à l'emploi — pas de saisie libre, pour éviter une erreur de syntaxe dans un bloc location.",
    "presets": {
      "blockDotfilesLabel": "Bloquer les fichiers cachés (.git, .htaccess...)",
      "blockDotfilesDescription": "Interdit l'accès direct aux fichiers/dossiers sensibles commençant par un point.",
      "cacheStaticLabel": "Cache long pour /assets/",
      "cacheStaticDescription": "Cache navigateur d'un an pour les fichiers statiques versionnés (JS/CSS avec hash dans le nom, généré par Vite/Webpack).",
      "spaFallbackLabel": "Fallback SPA (React/Vue/Vite)",
      "spaFallbackDescription": "Toutes les routes inconnues renvoient index.html — nécessaire pour une application une-page avec routage côté client."
    },
    "serviceModeTitle": "Mode de service",
    "serviceModeDescription": "« Fichiers statiques » sert directement un dossier du serveur ; « Proxy » relaie les requêtes vers une autre adresse (ex : une application Node.js/Docker sur un port local).",
    "modeRoot": "Fichiers statiques",
    "modeProxy": "Proxy",
    "cancel": "Annuler",
    "submitting": "...",
    "createSite": "Créer le site",
    "apply": "Appliquer"
  }
```

Ajouter dans `apps/web/public/locales/en/nginx.json`, même position :
```json
  "guidedEditor": {
    "certImport": {
      "pasteButton": "Paste a certificate",
      "uploadButton": "Upload files",
      "pasteTitle": "Paste the certificate and key",
      "uploadTitle": "Upload files",
      "close": "Close",
      "certPemLabel": "Certificate (PEM, e.g. fullchain.pem)",
      "keyPemLabel": "Private key (PEM, e.g. privkey.pem)",
      "saving": "Saving…",
      "save": "Save",
      "certFileLabel": "Certificate file (.pem/.crt)",
      "keyFileLabel": "Private key file (.pem/.key)",
      "importing": "Importing…",
      "import": "Import",
      "success": "Certificate saved and paths filled in above."
    },
    "requiredFields": "File name and server_name are required.",
    "createTitle": "New site — guided configuration",
    "editTitle": "Guided editor",
    "close": "Close",
    "fileNameLabel": "File name (sites-available)",
    "serverNameLabel": "server_name",
    "listenPortLabel": "Listen port",
    "sslLabel": "Enable SSL (HTTPS)",
    "sslDescription": "Enables HTTPS for this site by referencing a certificate and key already present on the server.",
    "certPathLabel": "Certificate path",
    "certKeyPathLabel": "Private key path",
    "verify": "Verify",
    "verifying": "Verifying…",
    "certFound": "File found.",
    "certMissing": "File not found at this location.",
    "certNameMismatch": "The imported certificate was tied to the name \"{{oldName}}\" — re-import it for \"{{newName}}\" before saving.",
    "certNamePrompt": "Fill in the file name above to be able to import a certificate.",
    "maxBodySizeLabel": "Maximum request size",
    "maxBodySizeDescription": "Limits the size of files/forms sent by visitors (e.g. 20m for 20 MB). Leave empty to keep Nginx's default (1 MB).",
    "securityHeadersTitle": "Security headers",
    "headers": {
      "frameOptions": "Prevents the site from being displayed in an iframe on another domain (anti-clickjacking).",
      "contentTypeOptions": "Prevents the browser from guessing a file's type differently from what the server declares.",
      "referrerPolicy": "Limits the information sent to other sites when a visitor clicks an outbound link.",
      "hsts": "Forces browsers to always use HTTPS for this site. Only enable if SSL is properly configured.",
      "xssProtection": "Legacy browser protection against reflected XSS attacks (deprecated but still accepted).",
      "permissionsPolicy": "Disables access to the camera, microphone, geolocation, etc. for this site.",
      "contentSecurityPolicy": "Restricts allowed script/style sources (advanced XSS protection). Generic baseline value — adjust if the site loads external resources (fonts, CDN, etc.)."
    },
    "tlsAdvancedTitle": "Advanced TLS",
    "tlsAdvancedDescription": "Optional settings to harden/align the TLS configuration. Leave empty to keep Nginx's defaults. Values are usually pasted from an external generator (e.g. Mozilla SSL Config Generator).",
    "protocolsLabel": "Protocols (ssl_protocols)",
    "ciphersLabel": "Cipher suites (ssl_ciphers)",
    "sessionCacheLabel": "Session cache",
    "sessionTimeoutLabel": "Session timeout",
    "gzipLabel": "Gzip compression",
    "gzipDescription": "Compresses responses before sending to speed up loading (text, CSS, JS, JSON...).",
    "presetsTitle": "Predefined content blocks",
    "presetsDescription": "Common ready-to-use templates — no free text entry, to avoid a syntax error in a location block.",
    "presets": {
      "blockDotfilesLabel": "Block hidden files (.git, .htaccess...)",
      "blockDotfilesDescription": "Denies direct access to sensitive files/folders starting with a dot.",
      "cacheStaticLabel": "Long cache for /assets/",
      "cacheStaticDescription": "One-year browser cache for versioned static files (JS/CSS with a hash in the name, generated by Vite/Webpack).",
      "spaFallbackLabel": "SPA fallback (React/Vue/Vite)",
      "spaFallbackDescription": "All unknown routes return index.html — required for a single-page app with client-side routing."
    },
    "serviceModeTitle": "Serving mode",
    "serviceModeDescription": "\"Static files\" serves a server folder directly; \"Proxy\" forwards requests to another address (e.g. a Node.js/Docker app on a local port).",
    "modeRoot": "Static files",
    "modeProxy": "Proxy",
    "cancel": "Cancel",
    "submitting": "...",
    "createSite": "Create site",
    "apply": "Apply"
  }
```

Ajouter dans `apps/web/public/locales/ta/nginx.json`, même position :
```json
  "guidedEditor": {
    "certImport": {
      "pasteButton": "சான்றிதழை ஒட்டு",
      "uploadButton": "கோப்புகளை பதிவேற்று",
      "pasteTitle": "சான்றிதழையும் சாவியையும் ஒட்டவும்",
      "uploadTitle": "கோப்புகளை பதிவேற்று",
      "close": "மூடு",
      "certPemLabel": "சான்றிதழ் (PEM, எ.கா. fullchain.pem)",
      "keyPemLabel": "தனிப்பட்ட சாவி (PEM, எ.கா. privkey.pem)",
      "saving": "சேமிக்கிறது…",
      "save": "சேமி",
      "certFileLabel": "சான்றிதழ் கோப்பு (.pem/.crt)",
      "keyFileLabel": "தனிப்பட்ட சாவி கோப்பு (.pem/.key)",
      "importing": "இறக்குமதி செய்கிறது…",
      "import": "இறக்குமதி",
      "success": "சான்றிதழ் சேமிக்கப்பட்டது, பாதைகள் மேலே நிரப்பப்பட்டுள்ளன."
    },
    "requiredFields": "கோப்பு பெயர் மற்றும் server_name தேவை.",
    "createTitle": "புதிய தளம் — வழிகாட்டப்பட்ட கட்டமைப்பு",
    "editTitle": "வழிகாட்டப்பட்ட திருத்தி",
    "close": "மூடு",
    "fileNameLabel": "கோப்பு பெயர் (sites-available)",
    "serverNameLabel": "server_name",
    "listenPortLabel": "கேட்கும் போர்ட்",
    "sslLabel": "SSL ஐ இயக்கு (HTTPS)",
    "sslDescription": "சேவையகத்தில் ஏற்கனவே உள்ள சான்றிதழ் மற்றும் சாவியைக் குறிப்பிட்டு இந்த தளத்திற்கு HTTPS ஐ இயக்குகிறது.",
    "certPathLabel": "சான்றிதழ் பாதை",
    "certKeyPathLabel": "தனிப்பட்ட சாவி பாதை",
    "verify": "சரிபார்",
    "verifying": "சரிபார்க்கிறது…",
    "certFound": "கோப்பு கண்டறியப்பட்டது.",
    "certMissing": "இந்த இடத்தில் கோப்பு கண்டறியப்படவில்லை.",
    "certNameMismatch": "இறக்குமதி செய்யப்பட்ட சான்றிதழ் « {{oldName}} » என்ற பெயருடன் இணைக்கப்பட்டிருந்தது — சேமிக்கும் முன் « {{newName}} » க்காக மீண்டும் இறக்குமதி செய்யவும்.",
    "certNamePrompt": "சான்றிதழை இறக்குமதி செய்ய மேலே கோப்பு பெயரை நிரப்பவும்.",
    "maxBodySizeLabel": "அதிகபட்ச கோரிக்கை அளவு",
    "maxBodySizeDescription": "பார்வையாளர்களால் அனுப்பப்படும் கோப்புகள்/படிவங்களின் அளவை வரம்பிடுகிறது (எ.கா. 20 MB க்கு 20m). Nginx இன் இயல்புநிலை மதிப்பை (1 MB) வைத்திருக்க காலியாக விடவும்.",
    "securityHeadersTitle": "பாதுகாப்பு தலைப்புகள்",
    "headers": {
      "frameOptions": "மற்றொரு டொமைனில் உள்ள iframe இல் தளம் காட்டப்படுவதைத் தடுக்கிறது (clickjacking எதிர்ப்பு).",
      "contentTypeOptions": "சேவையகம் அறிவிப்பதிலிருந்து வேறுபட்ட வகையாக ஒரு கோப்பை உலாவி யூகிப்பதைத் தடுக்கிறது.",
      "referrerPolicy": "பார்வையாளர் ஒரு வெளிச்செல்லும் இணைப்பைக் கிளிக் செய்யும்போது மற்ற தளங்களுக்கு அனுப்பப்படும் தகவலை வரம்பிடுகிறது.",
      "hsts": "இந்த தளத்திற்கு எப்போதும் HTTPS ஐப் பயன்படுத்த உலாவிகளை கட்டாயப்படுத்துகிறது. SSL சரியாக கட்டமைக்கப்பட்டிருந்தால் மட்டும் இயக்கவும்.",
      "xssProtection": "பிரதிபலித்த XSS தாக்குதல்களுக்கு எதிரான பழைய உலாவி பாதுகாப்பு (deprecated ஆனால் இன்னும் ஏற்கப்படுகிறது).",
      "permissionsPolicy": "இந்த தளத்திற்கு கேமரா, மைக், புவிஇருப்பிடம் போன்றவற்றுக்கான அணுகலை முடக்குகிறது.",
      "contentSecurityPolicy": "அனுமதிக்கப்பட்ட ஸ்கிரிப்ட்/ஸ்டைல் மூலங்களை கட்டுப்படுத்துகிறது (மேம்பட்ட XSS பாதுகாப்பு). அடிப்படை பொதுவான மதிப்பு — தளம் வெளிப்புற மூலங்களை (எழுத்துருக்கள், CDN, முதலியன) ஏற்றினால் மாற்றியமைக்கவும்."
    },
    "tlsAdvancedTitle": "மேம்பட்ட TLS",
    "tlsAdvancedDescription": "TLS கட்டமைப்பை பலப்படுத்த/சீரமைக்க விருப்ப அமைப்புகள். Nginx இன் இயல்புநிலை மதிப்புகளை வைத்திருக்க காலியாக விடவும். மதிப்புகள் பொதுவாக வெளிப்புற ஜெனரேட்டரிலிருந்து ஒட்டப்படுகின்றன (எ.கா. Mozilla SSL Config Generator).",
    "protocolsLabel": "நெறிமுறைகள் (ssl_protocols)",
    "ciphersLabel": "குறியாக்க தொகுப்புகள் (ssl_ciphers)",
    "sessionCacheLabel": "அமர்வு தேக்ககம்",
    "sessionTimeoutLabel": "அமர்வு காலம்",
    "gzipLabel": "Gzip குறுக்கம்",
    "gzipDescription": "ஏற்றுவதை விரைவுபடுத்த அனுப்பும் முன் பதில்களை குறுக்குகிறது (உரை, CSS, JS, JSON...).",
    "presetsTitle": "முன்னரே வரையறுக்கப்பட்ட உள்ளடக்க தொகுதிகள்",
    "presetsDescription": "பயன்பாட்டிற்கு தயாராக உள்ள பொதுவான வார்ப்புருக்கள் — location தொகுதியில் தொடரியல் பிழையைத் தவிர்க்க இலவச உரை உள்ளீடு இல்லை.",
    "presets": {
      "blockDotfilesLabel": "மறைக்கப்பட்ட கோப்புகளைத் தடு (.git, .htaccess...)",
      "blockDotfilesDescription": "புள்ளியுடன் தொடங்கும் முக்கியமான கோப்புகள்/கோப்புறைகளுக்கு நேரடி அணுகலை மறுக்கிறது.",
      "cacheStaticLabel": "/assets/ க்கு நீண்ட தேக்ககம்",
      "cacheStaticDescription": "பதிப்பு எண்ணிடப்பட்ட நிலையான கோப்புகளுக்கு (பெயரில் ஹாஷுடன் கூடிய JS/CSS, Vite/Webpack ஆல் உருவாக்கப்பட்டது) ஒரு வருட உலாவி தேக்ககம்.",
      "spaFallbackLabel": "SPA fallback (React/Vue/Vite)",
      "spaFallbackDescription": "அறியப்படாத அனைத்து பாதைகளும் index.html ஐ திருப்பி அனுப்பும் — கிளையன்ட்-பக்க வழிசெலுத்தலுடன் கூடிய ஒரு-பக்க பயன்பாட்டிற்கு தேவை."
    },
    "serviceModeTitle": "சேவை பயன்முறை",
    "serviceModeDescription": "« நிலையான கோப்புகள் » சேவையகத்தின் ஒரு கோப்புறையை நேரடியாக வழங்குகிறது; « ப்ராக்ஸி » கோரிக்கைகளை மற்றொரு முகவரிக்கு அனுப்புகிறது (எ.கா. உள்ளூர் போர்ட்டில் ஒரு Node.js/Docker பயன்பாடு).",
    "modeRoot": "நிலையான கோப்புகள்",
    "modeProxy": "ப்ராக்ஸி",
    "cancel": "ரத்துசெய்",
    "submitting": "...",
    "createSite": "தளத்தை உருவாக்கு",
    "apply": "பயன்படுத்து"
  }
```

- [ ] **Step 2: Traduire `HeaderCheckbox`, `CertImportPanel`, `GuidedConfigEditorDialog`**

Remplacer les lignes 603 à la fin du fichier (`const DEFAULT_GUIDED_MODEL` jusqu'à la fermeture finale) par :
```tsx
const DEFAULT_GUIDED_MODEL: GuidedFormModel = {
  sslEnabled: false,
  certPath: null,
  certKeyPath: null,
  clientMaxBodySize: null,
  headers: {
    frameOptions: false,
    contentTypeOptions: false,
    referrerPolicy: false,
    hsts: false,
    xssProtection: false,
    permissionsPolicy: false,
    contentSecurityPolicy: false,
  },
  mode: "root",
  rootPath: null,
  proxyPassTarget: null,
  tls: { protocols: null, ciphers: null, sessionCache: null, sessionTimeout: null },
  gzip: { enabled: false, types: null },
  locations: { blockDotfiles: false, cacheStaticAssets: false, spaFallback: false },
};

type GuidedConfigEditorDialogProps =
  | {
      mode: "edit";
      vhostName: string;
      content: string;
      trigger: React.ReactNode;
      onApply: (newContent: string) => void;
    }
  | {
      mode: "create";
      trigger: React.ReactNode;
      onCreated: () => void;
    };

/**
 * Guided editor for the v1 field set (SSL toggle + cert paths,
 * client_max_body_size, common security headers, root/proxy_pass mode) —
 * checkboxes/inputs instead of hand-writing nginx directives, with a short
 * explanation under each field. Never writes anything on its own: in "edit"
 * mode it only produces a new candidate `content` string handed back to the
 * parent via onApply (the existing "Enregistrer" button does the actual
 * save through the already-validated PUT .../config path); in "create" mode
 * it calls POST /nginx/vhosts directly since there's no existing file/save
 * button to defer to.
 */
function HeaderCheckbox({
  label,
  description,
  checked,
  onChange,
}: {
  label: string;
  description: string;
  checked: boolean;
  onChange: (checked: boolean) => void;
}) {
  return (
    <label className="flex items-start gap-2">
      <input type="checkbox" className="mt-0.5" checked={checked} onChange={(e) => onChange(e.target.checked)} />
      <span>
        <span className="block">{label}</span>
        <span className="block text-xs text-muted-foreground">{description}</span>
      </span>
    </label>
  );
}

/**
 * Cert import UI for the guided editor's SSL section — only shown in "edit"
 * mode (a real vhostName is required to key the managed-certs folder; a
 * brand-new vhost doesn't have one yet). Two independent paths: paste raw
 * PEM text, or upload cert/key files — both land on the same backend
 * endpoint family and, on success, overwrite certPath/certKeyPath in the
 * form model so the admin doesn't have to copy the resulting paths by hand.
 */
function CertImportPanel({
  vhostName,
  onImported,
}: {
  vhostName: string;
  onImported: (result: { certPath: string; keyPath: string }) => void;
}) {
  const { t } = useTranslation("nginx");
  const [mode, setMode] = useState<"closed" | "paste" | "upload">("closed");
  const [certPem, setCertPem] = useState("");
  const [keyPem, setKeyPem] = useState("");
  const [certFile, setCertFile] = useState<File | null>(null);
  const [keyFile, setKeyFile] = useState<File | null>(null);
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [success, setSuccess] = useState(false);

  async function submitPaste() {
    setBusy(true);
    setError(null);
    setSuccess(false);
    try {
      const result = await apiJson<{ certPath: string; keyPath: string }>("/nginx/cert-paths/import-text", {
        method: "POST",
        body: JSON.stringify({ vhostName, certPem, keyPem }),
      });
      onImported(result);
      setSuccess(true);
      setCertPem("");
      setKeyPem("");
    } catch (err) {
      setError((err as Error).message);
    } finally {
      setBusy(false);
    }
  }

  async function submitUpload() {
    if (!certFile || !keyFile) return;
    setBusy(true);
    setError(null);
    setSuccess(false);
    try {
      const form = new FormData();
      form.append("vhostName", vhostName);
      form.append("cert", certFile);
      form.append("key", keyFile);
      const res = await apiFetch("/nginx/cert-paths/import-file", { method: "POST", body: form });
      if (!res.ok) {
        const body = await res.json().catch(() => ({ error: "import_failed" }));
        throw new Error(body.error ?? "import_failed");
      }
      const result = await res.json();
      onImported(result);
      setSuccess(true);
      setCertFile(null);
      setKeyFile(null);
    } catch (err) {
      setError((err as Error).message);
    } finally {
      setBusy(false);
    }
  }

  if (mode === "closed") {
    return (
      <div className="mt-2 flex gap-2">
        <Button size="sm" variant="outline" onClick={() => setMode("paste")}>
          {t("guidedEditor.certImport.pasteButton")}
        </Button>
        <Button size="sm" variant="outline" onClick={() => setMode("upload")}>
          <Upload className="h-3.5 w-3.5" /> {t("guidedEditor.certImport.uploadButton")}
        </Button>
      </div>
    );
  }

  return (
    <div className="mt-2 rounded-md border border-border p-3">
      <div className="mb-2 flex items-center justify-between">
        <p className="text-xs font-medium">
          {mode === "paste" ? t("guidedEditor.certImport.pasteTitle") : t("guidedEditor.certImport.uploadTitle")}
        </p>
        <button type="button" onClick={() => setMode("closed")} className="text-muted-foreground hover:text-foreground">
          <X className="h-3.5 w-3.5" />
        </button>
      </div>

      {mode === "paste" && (
        <div className="flex flex-col gap-2">
          <div>
            <label className="mb-1 block text-xs text-muted-foreground">{t("guidedEditor.certImport.certPemLabel")}</label>
            <textarea
              value={certPem}
              onChange={(e) => setCertPem(e.target.value)}
              rows={4}
              placeholder="-----BEGIN CERTIFICATE-----"
              className="w-full rounded-md border border-border bg-background px-3 py-2 text-xs font-mono outline-none focus:ring-2 focus:ring-primary"
            />
          </div>
          <div>
            <label className="mb-1 block text-xs text-muted-foreground">{t("guidedEditor.certImport.keyPemLabel")}</label>
            <textarea
              value={keyPem}
              onChange={(e) => setKeyPem(e.target.value)}
              rows={4}
              placeholder="-----BEGIN PRIVATE KEY-----"
              className="w-full rounded-md border border-border bg-background px-3 py-2 text-xs font-mono outline-none focus:ring-2 focus:ring-primary"
            />
          </div>
          <Button size="sm" disabled={busy || !certPem.trim() || !keyPem.trim()} onClick={submitPaste}>
            {busy ? t("guidedEditor.certImport.saving") : t("guidedEditor.certImport.save")}
          </Button>
        </div>
      )}

      {mode === "upload" && (
        <div className="flex flex-col gap-2">
          <div>
            <label className="mb-1 block text-xs text-muted-foreground">{t("guidedEditor.certImport.certFileLabel")}</label>
            <input
              type="file"
              accept=".pem,.crt,.cer"
              onChange={(e) => setCertFile(e.target.files?.[0] ?? null)}
              className="w-full text-xs"
            />
          </div>
          <div>
            <label className="mb-1 block text-xs text-muted-foreground">{t("guidedEditor.certImport.keyFileLabel")}</label>
            <input
              type="file"
              accept=".pem,.key"
              onChange={(e) => setKeyFile(e.target.files?.[0] ?? null)}
              className="w-full text-xs"
            />
          </div>
          <Button size="sm" disabled={busy || !certFile || !keyFile} onClick={submitUpload}>
            {busy ? t("guidedEditor.certImport.importing") : t("guidedEditor.certImport.import")}
          </Button>
        </div>
      )}

      {error && <p className="mt-2 text-xs text-destructive">{error}</p>}
      {success && (
        <p className="mt-2 flex items-center gap-1 text-xs text-primary">
          <CheckCircle2 className="h-3.5 w-3.5" /> {t("guidedEditor.certImport.success")}
        </p>
      )}
    </div>
  );
}

function GuidedConfigEditorDialog(props: GuidedConfigEditorDialogProps) {
  const { t } = useTranslation("nginx");
  const [open, setOpen] = useState(false);
  const [model, setModel] = useState<GuidedFormModel>(DEFAULT_GUIDED_MODEL);
  const [serverName, setServerName] = useState("");
  const [listenPort, setListenPort] = useState(80);
  const [newVhostName, setNewVhostName] = useState("");
  // Cert import (create mode) is keyed by the "Nom du fichier" field at
  // import time — if the admin edits that field afterward, the already-
  // filled certPath/certKeyPath silently point at the WRONG managed-cert
  // folder (data/nginx-certs/<old-name>/) unless flagged.
  const [certImportedForName, setCertImportedForName] = useState<string | null>(null);
  const [certCheck, setCertCheck] = useState<"idle" | "checking" | "found" | "missing">("idle");
  const [error, setError] = useState<string | null>(null);
  const [saving, setSaving] = useState(false);

  useEffect(() => {
    if (!open) return;
    setError(null);
    setCertCheck("idle");
    if (props.mode === "edit") {
      apiJson<GuidedFormModel>(`/nginx/vhosts/${props.vhostName}/guided/parse`, {
        method: "POST",
        body: JSON.stringify({ content: props.content }),
      })
        .then(setModel)
        .catch((err) => setError((err as Error).message));
    } else {
      setModel(DEFAULT_GUIDED_MODEL);
      setServerName("");
      setListenPort(80);
      setNewVhostName("");
      setCertImportedForName(null);
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [open]);

  async function checkCertPath() {
    if (!model.certPath) return;
    setCertCheck("checking");
    try {
      const { exists } = await apiJson<{ exists: boolean }>(
        `/nginx/cert-paths/check?path=${encodeURIComponent(model.certPath)}`
      );
      setCertCheck(exists ? "found" : "missing");
    } catch {
      setCertCheck("missing");
    }
  }

  function applyImportedCertPaths(result: { certPath: string; keyPath: string }) {
    setCertCheck("idle");
    setModel((m) => ({ ...m, certPath: result.certPath, certKeyPath: result.keyPath }));
    if (props.mode === "create") setCertImportedForName(newVhostName.trim());
  }

  async function submit() {
    setSaving(true);
    setError(null);
    try {
      if (props.mode === "edit") {
        const { content: newContent } = await apiJson<{ content: string }>(
          `/nginx/vhosts/${props.vhostName}/guided/apply`,
          { method: "POST", body: JSON.stringify({ content: props.content, model }) }
        );
        props.onApply(newContent);
        setOpen(false);
      } else {
        if (!newVhostName.trim() || !serverName.trim()) {
          setError(t("guidedEditor.requiredFields"));
          return;
        }
        const { content } = await apiJson<{ content: string }>("/nginx/vhosts/guided/build", {
          method: "POST",
          body: JSON.stringify({ model, serverName: serverName.trim(), listenPort }),
        });
        await apiJson("/nginx/vhosts", {
          method: "POST",
          body: JSON.stringify({ name: newVhostName.trim(), content }),
        });
        props.onCreated();
        setOpen(false);
      }
    } catch (err) {
      setError((err as Error).message);
    } finally {
      setSaving(false);
    }
  }

  return (
    <Dialog.Root open={open} onOpenChange={setOpen}>
      <Dialog.Trigger asChild>{props.trigger}</Dialog.Trigger>
      <Dialog.Portal>
        <Dialog.Overlay className="fixed inset-0 z-40 bg-black/50" />
        <Dialog.Content className="fixed left-1/2 top-1/2 z-50 max-h-[85vh] w-[92vw] max-w-lg -translate-x-1/2 -translate-y-1/2 overflow-y-auto rounded-lg border border-border bg-card p-4 shadow-xl outline-none">
          <div className="flex items-center justify-between">
            <Dialog.Title className="text-base font-semibold">
              {props.mode === "create" ? t("guidedEditor.createTitle") : t("guidedEditor.editTitle")}
            </Dialog.Title>
            <Dialog.Close className="rounded-md p-1 hover:bg-muted" aria-label={t("guidedEditor.close")}>
              <X className="h-4 w-4" />
            </Dialog.Close>
          </div>

          <div className="mt-3 flex flex-col gap-4 text-sm">
            {props.mode === "create" && (
              <div className="flex flex-col gap-2">
                <div>
                  <label className="mb-1 block text-xs font-medium">{t("guidedEditor.fileNameLabel")}</label>
                  <input
                    type="text"
                    value={newVhostName}
                    onChange={(e) => setNewVhostName(e.target.value)}
                    placeholder="mon-site"
                    className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-primary"
                  />
                </div>
                <div>
                  <label className="mb-1 block text-xs font-medium">{t("guidedEditor.serverNameLabel")}</label>
                  <input
                    type="text"
                    value={serverName}
                    onChange={(e) => setServerName(e.target.value)}
                    placeholder="exemple.fr www.exemple.fr"
                    className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-primary"
                  />
                </div>
                <div>
                  <label className="mb-1 block text-xs font-medium">{t("guidedEditor.listenPortLabel")}</label>
                  <input
                    type="number"
                    value={listenPort}
                    onChange={(e) => setListenPort(Number(e.target.value) || 80)}
                    className="w-32 rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-primary"
                  />
                </div>
              </div>
            )}

            <div>
              <label className="flex items-center gap-2 font-medium">
                <input
                  type="checkbox"
                  checked={model.sslEnabled}
                  onChange={(e) => setModel((m) => ({ ...m, sslEnabled: e.target.checked }))}
                />
                {t("guidedEditor.sslLabel")}
              </label>
              <p className="mt-0.5 text-xs text-muted-foreground">{t("guidedEditor.sslDescription")}</p>
              {model.sslEnabled && (
                <div className="mt-2 flex flex-col gap-2 pl-6">
                  <div>
                    <label className="mb-1 block text-xs text-muted-foreground">{t("guidedEditor.certPathLabel")}</label>
                    <div className="flex gap-2">
                      <input
                        type="text"
                        value={model.certPath ?? ""}
                        onChange={(e) => {
                          setCertCheck("idle");
                          setModel((m) => ({ ...m, certPath: e.target.value || null }));
                        }}
                        placeholder="/etc/letsencrypt/live/exemple.fr/fullchain.pem"
                        className="min-w-0 flex-1 rounded-md border border-border bg-background px-3 py-2 text-xs font-mono outline-none focus:ring-2 focus:ring-primary"
                      />
                      <Button size="sm" variant="outline" disabled={!model.certPath} onClick={checkCertPath}>
                        {t("guidedEditor.verify")}
                      </Button>
                    </div>
                    {certCheck === "checking" && <p className="mt-1 text-xs text-muted-foreground">{t("guidedEditor.verifying")}</p>}
                    {certCheck === "found" && (
                      <p className="mt-1 flex items-center gap-1 text-xs text-primary">
                        <CheckCircle2 className="h-3 w-3" /> {t("guidedEditor.certFound")}
                      </p>
                    )}
                    {certCheck === "missing" && (
                      <p className="mt-1 flex items-center gap-1 text-xs text-destructive">
                        <XCircle className="h-3 w-3" /> {t("guidedEditor.certMissing")}
                      </p>
                    )}
                  </div>
                  <div>
                    <label className="mb-1 block text-xs text-muted-foreground">{t("guidedEditor.certKeyPathLabel")}</label>
                    <input
                      type="text"
                      value={model.certKeyPath ?? ""}
                      onChange={(e) => setModel((m) => ({ ...m, certKeyPath: e.target.value || null }))}
                      placeholder="/etc/letsencrypt/live/exemple.fr/privkey.pem"
                      className="w-full rounded-md border border-border bg-background px-3 py-2 text-xs font-mono outline-none focus:ring-2 focus:ring-primary"
                    />
                  </div>
                  {props.mode === "edit" ? (
                    <CertImportPanel vhostName={props.vhostName} onImported={applyImportedCertPaths} />
                  ) : newVhostName.trim() ? (
                    <>
                      {certImportedForName && certImportedForName !== newVhostName.trim() && (
                        <p className="mt-2 flex items-center gap-1 text-xs text-warning">
                          <AlertTriangle className="h-3.5 w-3.5" />
                          {t("guidedEditor.certNameMismatch", {
                            oldName: certImportedForName,
                            newName: newVhostName.trim(),
                          })}
                        </p>
                      )}
                      <CertImportPanel vhostName={newVhostName.trim()} onImported={applyImportedCertPaths} />
                    </>
                  ) : (
                    <p className="mt-2 text-xs text-muted-foreground">{t("guidedEditor.certNamePrompt")}</p>
                  )}
                </div>
              )}
            </div>

            <div>
              <label className="mb-1 block font-medium">{t("guidedEditor.maxBodySizeLabel")}</label>
              <p className="mb-1 text-xs text-muted-foreground">{t("guidedEditor.maxBodySizeDescription")}</p>
              <input
                type="text"
                value={model.clientMaxBodySize ?? ""}
                onChange={(e) => setModel((m) => ({ ...m, clientMaxBodySize: e.target.value || null }))}
                placeholder="20m"
                className="w-32 rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-primary"
              />
            </div>

            <div>
              <p className="mb-1 font-medium">{t("guidedEditor.securityHeadersTitle")}</p>
              <div className="flex flex-col gap-2">
                <HeaderCheckbox
                  label="X-Frame-Options"
                  description={t("guidedEditor.headers.frameOptions")}
                  checked={model.headers.frameOptions}
                  onChange={(v) => setModel((m) => ({ ...m, headers: { ...m.headers, frameOptions: v } }))}
                />
                <HeaderCheckbox
                  label="X-Content-Type-Options"
                  description={t("guidedEditor.headers.contentTypeOptions")}
                  checked={model.headers.contentTypeOptions}
                  onChange={(v) => setModel((m) => ({ ...m, headers: { ...m.headers, contentTypeOptions: v } }))}
                />
                <HeaderCheckbox
                  label="Referrer-Policy"
                  description={t("guidedEditor.headers.referrerPolicy")}
                  checked={model.headers.referrerPolicy}
                  onChange={(v) => setModel((m) => ({ ...m, headers: { ...m.headers, referrerPolicy: v } }))}
                />
                <HeaderCheckbox
                  label="Strict-Transport-Security (HSTS)"
                  description={t("guidedEditor.headers.hsts")}
                  checked={model.headers.hsts}
                  onChange={(v) => setModel((m) => ({ ...m, headers: { ...m.headers, hsts: v } }))}
                />
                <HeaderCheckbox
                  label="X-XSS-Protection"
                  description={t("guidedEditor.headers.xssProtection")}
                  checked={model.headers.xssProtection}
                  onChange={(v) => setModel((m) => ({ ...m, headers: { ...m.headers, xssProtection: v } }))}
                />
                <HeaderCheckbox
                  label="Permissions-Policy"
                  description={t("guidedEditor.headers.permissionsPolicy")}
                  checked={model.headers.permissionsPolicy}
                  onChange={(v) => setModel((m) => ({ ...m, headers: { ...m.headers, permissionsPolicy: v } }))}
                />
                <HeaderCheckbox
                  label="Content-Security-Policy"
                  description={t("guidedEditor.headers.contentSecurityPolicy")}
                  checked={model.headers.contentSecurityPolicy}
                  onChange={(v) => setModel((m) => ({ ...m, headers: { ...m.headers, contentSecurityPolicy: v } }))}
                />
              </div>
            </div>

            {model.sslEnabled && (
              <div>
                <p className="mb-1 font-medium">{t("guidedEditor.tlsAdvancedTitle")}</p>
                <p className="mb-1 text-xs text-muted-foreground">{t("guidedEditor.tlsAdvancedDescription")}</p>
                <div className="flex flex-col gap-2">
                  <div>
                    <label className="mb-1 block text-xs text-muted-foreground">{t("guidedEditor.protocolsLabel")}</label>
                    <input
                      type="text"
                      value={model.tls.protocols ?? ""}
                      onChange={(e) => setModel((m) => ({ ...m, tls: { ...m.tls, protocols: e.target.value || null } }))}
                      placeholder="TLSv1.2 TLSv1.3"
                      className="w-full rounded-md border border-border bg-background px-3 py-2 text-xs font-mono outline-none focus:ring-2 focus:ring-primary"
                    />
                  </div>
                  <div>
                    <label className="mb-1 block text-xs text-muted-foreground">{t("guidedEditor.ciphersLabel")}</label>
                    <input
                      type="text"
                      value={model.tls.ciphers ?? ""}
                      onChange={(e) => setModel((m) => ({ ...m, tls: { ...m.tls, ciphers: e.target.value || null } }))}
                      placeholder="ECDHE-ECDSA-AES128-GCM-SHA256:..."
                      className="w-full rounded-md border border-border bg-background px-3 py-2 text-xs font-mono outline-none focus:ring-2 focus:ring-primary"
                    />
                  </div>
                  <div className="flex gap-2">
                    <div className="flex-1">
                      <label className="mb-1 block text-xs text-muted-foreground">{t("guidedEditor.sessionCacheLabel")}</label>
                      <input
                        type="text"
                        value={model.tls.sessionCache ?? ""}
                        onChange={(e) =>
                          setModel((m) => ({ ...m, tls: { ...m.tls, sessionCache: e.target.value || null } }))
                        }
                        placeholder="shared:SSL:10m"
                        className="w-full rounded-md border border-border bg-background px-3 py-2 text-xs font-mono outline-none focus:ring-2 focus:ring-primary"
                      />
                    </div>
                    <div className="flex-1">
                      <label className="mb-1 block text-xs text-muted-foreground">{t("guidedEditor.sessionTimeoutLabel")}</label>
                      <input
                        type="text"
                        value={model.tls.sessionTimeout ?? ""}
                        onChange={(e) =>
                          setModel((m) => ({ ...m, tls: { ...m.tls, sessionTimeout: e.target.value || null } }))
                        }
                        placeholder="1d"
                        className="w-full rounded-md border border-border bg-background px-3 py-2 text-xs font-mono outline-none focus:ring-2 focus:ring-primary"
                      />
                    </div>
                  </div>
                </div>
              </div>
            )}

            <div>
              <label className="flex items-center gap-2 font-medium">
                <input
                  type="checkbox"
                  checked={model.gzip.enabled}
                  onChange={(e) => setModel((m) => ({ ...m, gzip: { ...m.gzip, enabled: e.target.checked } }))}
                />
                {t("guidedEditor.gzipLabel")}
              </label>
              <p className="mt-0.5 text-xs text-muted-foreground">{t("guidedEditor.gzipDescription")}</p>
              {model.gzip.enabled && (
                <input
                  type="text"
                  value={model.gzip.types ?? ""}
                  onChange={(e) => setModel((m) => ({ ...m, gzip: { ...m.gzip, types: e.target.value || null } }))}
                  placeholder="text/plain text/css application/javascript application/json"
                  className="mt-2 w-full rounded-md border border-border bg-background px-3 py-2 text-xs font-mono outline-none focus:ring-2 focus:ring-primary"
                />
              )}
            </div>

            <div>
              <p className="mb-1 font-medium">{t("guidedEditor.presetsTitle")}</p>
              <p className="mb-1 text-xs text-muted-foreground">
                {t("guidedEditor.presetsDescription")
                  .split(/(location)/)
                  .map((part, i) =>
                    part === "location" ? (
                      <span key={i} className="font-mono">
                        location
                      </span>
                    ) : (
                      part
                    )
                  )}
              </p>
              <div className="flex flex-col gap-2">
                <label className="flex items-start gap-2">
                  <input
                    type="checkbox"
                    className="mt-0.5"
                    checked={model.locations.blockDotfiles}
                    onChange={(e) =>
                      setModel((m) => ({ ...m, locations: { ...m.locations, blockDotfiles: e.target.checked } }))
                    }
                  />
                  <span>
                    <span className="block">{t("guidedEditor.presets.blockDotfilesLabel")}</span>
                    <span className="block text-xs text-muted-foreground">{t("guidedEditor.presets.blockDotfilesDescription")}</span>
                  </span>
                </label>
                <label className="flex items-start gap-2">
                  <input
                    type="checkbox"
                    className="mt-0.5"
                    checked={model.locations.cacheStaticAssets}
                    onChange={(e) =>
                      setModel((m) => ({ ...m, locations: { ...m.locations, cacheStaticAssets: e.target.checked } }))
                    }
                  />
                  <span>
                    <span className="block">{t("guidedEditor.presets.cacheStaticLabel")}</span>
                    <span className="block text-xs text-muted-foreground">{t("guidedEditor.presets.cacheStaticDescription")}</span>
                  </span>
                </label>
                <label className="flex items-start gap-2">
                  <input
                    type="checkbox"
                    className="mt-0.5"
                    checked={model.locations.spaFallback}
                    onChange={(e) =>
                      setModel((m) => ({ ...m, locations: { ...m.locations, spaFallback: e.target.checked } }))
                    }
                  />
                  <span>
                    <span className="block">{t("guidedEditor.presets.spaFallbackLabel")}</span>
                    <span className="block text-xs text-muted-foreground">{t("guidedEditor.presets.spaFallbackDescription")}</span>
                  </span>
                </label>
              </div>
            </div>

            <div>
              <p className="mb-1 font-medium">{t("guidedEditor.serviceModeTitle")}</p>
              <p className="mb-1 text-xs text-muted-foreground">{t("guidedEditor.serviceModeDescription")}</p>
              <div className="flex gap-4">
                <label className="flex items-center gap-1.5 text-xs">
                  <input
                    type="radio"
                    checked={model.mode === "root"}
                    onChange={() => setModel((m) => ({ ...m, mode: "root" }))}
                  />
                  {t("guidedEditor.modeRoot")}
                </label>
                <label className="flex items-center gap-1.5 text-xs">
                  <input
                    type="radio"
                    checked={model.mode === "proxy_pass"}
                    onChange={() => setModel((m) => ({ ...m, mode: "proxy_pass" }))}
                  />
                  {t("guidedEditor.modeProxy")}
                </label>
              </div>
              {model.mode === "root" && (
                <input
                  type="text"
                  value={model.rootPath ?? ""}
                  onChange={(e) => setModel((m) => ({ ...m, rootPath: e.target.value || null }))}
                  placeholder="/var/www/mon-site"
                  className="mt-2 w-full rounded-md border border-border bg-background px-3 py-2 text-xs font-mono outline-none focus:ring-2 focus:ring-primary"
                />
              )}
              {model.mode === "proxy_pass" && (
                <input
                  type="text"
                  value={model.proxyPassTarget ?? ""}
                  onChange={(e) => setModel((m) => ({ ...m, proxyPassTarget: e.target.value || null }))}
                  placeholder="http://127.0.0.1:3000"
                  className="mt-2 w-full rounded-md border border-border bg-background px-3 py-2 text-xs font-mono outline-none focus:ring-2 focus:ring-primary"
                />
              )}
            </div>

            {error && <p className="text-xs text-destructive">{error}</p>}
          </div>

          <div className="mt-4 flex justify-end gap-2">
            <Dialog.Close asChild>
              <Button variant="outline" size="sm" disabled={saving}>
                {t("guidedEditor.cancel")}
              </Button>
            </Dialog.Close>
            <Button size="sm" disabled={saving} onClick={submit}>
              {saving ? t("guidedEditor.submitting") : props.mode === "create" ? t("guidedEditor.createSite") : t("guidedEditor.apply")}
            </Button>
          </div>
        </Dialog.Content>
      </Dialog.Portal>
    </Dialog.Root>
  );
}
```

Note importante : le paragraphe "Modèles courants prêts à l'emploi..." contient un mot en `font-mono` (`location`) au milieu d'une phrase (l.1190-1192 de l'original : `Modèles courants prêts à l'emploi — pas de saisie libre, pour éviter une erreur de syntaxe dans un bloc <span className="font-mono">location</span>.`). Puisque `location` est un mot-clé technique nginx qui ne doit pas être traduit ni perdre son style monospace, la clé `presetsDescription` dans les 3 JSON contient le mot "location" en clair dans le texte (pas de balise), et le code ci-dessus fait un `.split(/(location)/)` sur le texte traduit pour ré-appliquer dynamiquement le style `font-mono` autour de ce mot précis, quelle que soit la langue. C'est une solution pragmatique pour ce cas isolé (un seul endroit dans tout le projet a ce besoin) — ne pas généraliser ce pattern ailleurs sans nécessité similaire. Si le mot "location" n'apparaît pas dans une traduction (ne devrait pas arriver vu que ce plan garde "location" identique dans les 3 langues), le `.split` renvoie simplement le texte complet sans callback déclenché, sans erreur.

- [ ] **Step 3: Vérifier le typecheck**

Run: `npx tsc --noEmit -p apps/web/tsconfig.json`
Expected: aucune erreur (hors bruit préexistant `ImaNote.tsx`).

- [ ] **Step 4: Vérifier la validité JSON et la parité des clés des 3 fichiers `nginx.json` complets**

Run:
```bash
node -e "
const fs = require('fs');
function flatten(obj, prefix = '') {
  let keys = [];
  for (const k in obj) {
    const path = prefix ? prefix + '.' + k : k;
    if (typeof obj[k] === 'object' && obj[k] !== null) {
      keys = keys.concat(flatten(obj[k], path));
    } else {
      keys.push(path);
    }
  }
  return keys.sort();
}
const langs = ['fr', 'en', 'ta'];
const keysets = {};
for (const lang of langs) {
  const content = JSON.parse(fs.readFileSync(\`apps/web/public/locales/\${lang}/nginx.json\`, 'utf-8'));
  keysets[lang] = flatten(content).join(',');
}
console.log(keysets.fr === keysets.en && keysets.en === keysets.ta ? 'OK: key sets match' : 'MISMATCH');
"
```
Expected: `OK: key sets match` — les 3 fichiers (combinant les clés de Task 1 et Task 2) doivent avoir une structure identique.

- [ ] **Step 5: Build de production et vérification**

Run (depuis la racine du repo, avec le contournement `ImaNote.tsx` si nécessaire) :
```bash
npm run build --workspace=packages/shared
npm run build:api
npm run build:web
```
Expected : les 3 builds réussissent. Vérifier que `apps/web/dist/locales/{fr,en,ta}/nginx.json` existent et contiennent bien la clé `guidedEditor` en plus des clés de Task 1.

- [ ] **Step 6: Test manuel de régression**

Run: `npm run dev:web`, naviguer sur `/nginx`, ouvrir le dialogue "Nouveau site" (mode création) en français puis en anglais et tamoul — vérifier que tous les champs, labels, descriptions, en-têtes de sécurité et blocs prédéfinis sont traduits, que le mot "location" dans la description des blocs prédéfinis garde son style monospace dans les 3 langues, qu'aucune clé de traduction brute n'apparaît. Si un serveur dev est lancé, vérifier qu'il est bien arrêté avant de terminer.

- [ ] **Step 7: Commit**

```bash
git add apps/web/public/locales/fr/nginx.json apps/web/public/locales/en/nginx.json apps/web/public/locales/ta/nginx.json apps/web/src/routes/Nginx.tsx
git commit -m "feat(web): traduire l'éditeur guidé Nginx via i18next"
```

---

## Fin de ce plan

Écran Nginx entièrement traduit (statut, vhosts, panneau de détail, éditeur guidé complet). Ceci termine le lot "management" du spec i18n (Pm2, Security, OsSystem, NetworkSecurity, Sites, Docker, Nginx tous traduits). Prochaine étape : lot "ops" (Wizard + composants, Applications, Backups, Restore, UsbExplorer, System, Services).
