# Multi-langue — Écran Backups Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Traduire en FR/EN/TA l'écran `Backups.tsx` (1120 lignes), troisième écran du lot "ops" du spec i18n après Wizard et Applications.

**Architecture:** Un seul namespace `backups` pour tout l'écran. Scindé en 2 tâches vu la taille : Task 1 couvre l'écran principal (`Backups`, `NewJobForm`, `DriveStatusBadge`) ; Task 2 couvre les cartes secondaires (`GDriveCompareCard`, `GDriveCompareSummary`, `OrphanGroupCard`, `UsbConnection`, `MigrationSnapshotCard`).

**Tech Stack:** react-i18next (déjà installé et opérationnel), TypeScript, Vite.

**Spec:** [docs/superpowers/specs/2026-08-15-i18n-frontend-design.md](../specs/2026-08-15-i18n-frontend-design.md)

## Global Constraints

- Langues supportées : `fr` (défaut/fallback), `en`, `ta`. Namespace unique `backups` sous `apps/web/public/locales/<lng>/backups.json`.
- Le namespace `backups` doit être ajouté à la liste `ns` de `apps/web/src/lib/i18n.ts` — lire l'état actuel avant de modifier (ajout dans Task 1 seulement).
- `formatBytes` importé depuis `./Docker` (déjà traduit, signature inchangée) — ne pas y toucher.
- Les chaînes interpolées utilisent l'interpolation i18next, jamais de concaténation.
- Les valeurs techniques (noms de job/volume/base de données, statuts bruts type "success"/"failed", chemins de fichiers, expressions cron, cibles `local`/`gdrive`/`usb` affichées telles quelles) ne sont PAS traduites.
- Après chaque tâche : `npx tsc --noEmit -p apps/web/tsconfig.json` (depuis la racine du repo) doit passer sans erreur. Note : fichier privé non suivi `apps/web/src/routes/ImaNote.tsx` cause des erreurs TS2305 préexistantes sans rapport — déplacer temporairement si besoin d'un signal propre.
- Aucun changement backend — hors périmètre.
- Tout le contenu traité est du texte d'interface original de cette application.

---

## Task 1: Traduire `Backups`, `NewJobForm`, `DriveStatusBadge`

**Files:**
- Create: `apps/web/public/locales/fr/backups.json`
- Create: `apps/web/public/locales/en/backups.json`
- Create: `apps/web/public/locales/ta/backups.json`
- Modify: `apps/web/src/routes/Backups.tsx` (lignes 1-710 du fichier actuel : `Backups`, `NewJobForm`, `DriveStatusBadge` — le reste, `GDriveCompareCard`/`GDriveCompareSummary`/`OrphanGroupCard`/`UsbConnection`/`MigrationSnapshotCard`, reste identique à l'original, Task 2 s'en charge)
- Modify: `apps/web/src/lib/i18n.ts`

**Interfaces:**
- Consumes: `useTranslation("backups")`, `formatBytes` de `./Docker` (inchangé).
- Produces: les 3 fichiers `backups.json` créés ici seront complétés par Task 2 (ajout de clés sans toucher aux clés créées ici).

Chaînes à extraire, par composant (voir contenu du fichier lu en entier plus haut pour les numéros de ligne exacts) :

**Module top-level** : `STATUS_FILTERS` (l.40-45) avec 4 labels "Tous"/"Succès"/"Échec"/"En cours" — devient `labelKey` résolu au rendu (même pattern que `TABS`/`navItems.ts` des écrans précédents).

**`Backups`** : "Stockage local" (l.165), "Nouvelle sauvegarde" (l.183), boutons "Annuler"/"Nouveau job" (l.185), "Chargement…"/"Aucun job configuré." (l.199-200), "cibles : {{targets}}" avec suffixe cron optionnel (l.210-211), titre confirmation suppression job "Supprimer le job {{name}} ?"/"Supprimer" (l.224-225, interpolé), titre+description carte bases de données "Sauvegarde partielle — bases de données détectées"/texte explicatif (l.237-241), libellés "conteneur"/"hôte" (l.251), bouton "Dump"/"Dump puis télécharger" (l.261, 268), "Bases : {{list}}" (l.275, interpolé), "Aucune base applicative (hors bases système)" (l.278), "Aucune base détectée." (l.284), "Historique" (l.290), texte lien restauration "Pour restaurer une sauvegarde, rendez-vous sur"/"Restore" (l.305-307), "Aucun historique." (l.313), titre+description confirmation suppression sauvegarde "Supprimer cette sauvegarde ?"/description/"Supprimer" (l.363-365), `title="Télécharger"` (l.353).

**`NewJobForm`** : "Étape 1 — Type" (l.493), boutons "Complet"/"Partiel (base de données)" (l.503, 513), options select "volume Docker"/"dossier (bind mount)" (l.525-526), placeholder "Nom du job" (l.530, réutilisé l.597), messages d'erreur de chargement (l.424, 429, 434 — volumes/bases de données/dossiers montés), placeholders conditionnels "Chargement…"/"Référence source (nom du volume)"/"Chemin absolu (ex: /opt/docker-data/mon-app)" (l.541-545), lien "Choisir dans la liste" (l.556), options "Sélectionner un volume…"/"Sélectionner un dossier monté…" (l.568), lien "Saisir manuellement" (l.581), texte explicatif backup partiel (l.588-590), placeholder "Référence source (location:ref, ex: native:mariadb)" (l.606), option "Sélectionner une base de données…" (l.628), "Étape 2 — Destination" (l.648), labels checkbox "local"/"gdrive"/"(non connecté)"/"usb"/"(non configuré — voir plus bas)"/"télécharger après la sauvegarde" (l.652, 661-662, 671-672, 676), placeholder "Expression cron (optionnel)" (l.683), message de validation "Nom, référence et au moins une cible sont requis" (l.462), bouton "Création…"/"Créer" (l.690).

**`DriveStatusBadge`** : 4 labels de statut "vérifié sur Drive"/"taille différente sur Drive"/"absent de Drive"/"jamais envoyé sur Drive" (l.699-702).

- [ ] **Step 1: Créer les fichiers `backups.json` (première moitié — Task 2 les complétera)**

`apps/web/public/locales/fr/backups.json`:
```json
{
  "statusFilters": {
    "all": "Tous",
    "success": "Succès",
    "failed": "Échec",
    "running": "En cours"
  },
  "localStorage": "Stockage local",
  "newBackup": "Nouvelle sauvegarde",
  "cancel": "Annuler",
  "newJob": "Nouveau job",
  "loading": "Chargement…",
  "noJobConfigured": "Aucun job configuré.",
  "targetsLabel": "cibles : {{targets}}",
  "deleteJobConfirm": {
    "title": "Supprimer le job {{name}} ?",
    "confirmLabel": "Supprimer"
  },
  "detectedDbs": {
    "title": "Sauvegarde partielle — bases de données détectées",
    "explanation": "Un dump de base de données est rapide et se comporte comme une sauvegarde « partielle » — à la différence d'un instantané complet de volume/dossier.",
    "locationContainer": "conteneur",
    "locationHost": "hôte",
    "dump": "Dump",
    "dumpAndDownloadTitle": "Dump puis télécharger",
    "databasesLabel": "Bases : {{list}}",
    "noAppDatabases": "Aucune base applicative (hors bases système)",
    "none": "Aucune base détectée."
  },
  "history": {
    "title": "Historique",
    "restorePrompt": "Pour restaurer une sauvegarde, rendez-vous sur",
    "restoreLink": "Restore",
    "empty": "Aucun historique.",
    "downloadTitle": "Télécharger",
    "deleteConfirm": {
      "title": "Supprimer cette sauvegarde ?",
      "description": "Le fichier local sera supprimé (les copies USB/Drive existantes ne sont pas touchées). Action irréversible.",
      "confirmLabel": "Supprimer"
    }
  },
  "newJobForm": {
    "step1Title": "Étape 1 — Type",
    "kindFull": "Complet",
    "kindPartial": "Partiel (base de données)",
    "sourceTypeVolume": "volume Docker",
    "sourceTypePath": "dossier (bind mount)",
    "jobNamePlaceholder": "Nom du job",
    "loadVolumesError": "Impossible de charger la liste des volumes",
    "loadDbsError": "Impossible de charger la liste des bases de données",
    "loadMountsError": "Impossible de charger la liste des dossiers montés",
    "sourceRefVolumePlaceholder": "Référence source (nom du volume)",
    "sourceRefPathPlaceholder": "Chemin absolu (ex: /opt/docker-data/mon-app)",
    "chooseFromList": "Choisir dans la liste",
    "selectVolumePrompt": "Sélectionner un volume…",
    "selectMountPrompt": "Sélectionner un dossier monté…",
    "enterManually": "Saisir manuellement",
    "partialExplanation": "Pour un dump ponctuel, utilisez le bouton « Dump » dans la carte « Bases de données détectées » ci-dessous. Ce formulaire crée un job planifiable — la source est une base de données détectée.",
    "sourceRefDbPlaceholder": "Référence source (location:ref, ex: native:mariadb)",
    "selectDbPrompt": "Sélectionner une base de données…",
    "step2Title": "Étape 2 — Destination",
    "targetLocal": "local",
    "targetGdrive": "gdrive",
    "targetGdriveNotConnected": "(non connecté)",
    "targetUsb": "usb",
    "targetUsbNotConfigured": "(non configuré — voir plus bas)",
    "downloadAfter": "télécharger après la sauvegarde",
    "cronPlaceholder": "Expression cron (optionnel)",
    "validationError": "Nom, référence et au moins une cible sont requis",
    "creating": "Création…",
    "create": "Créer"
  },
  "driveStatus": {
    "verified": "vérifié sur Drive",
    "sizeMismatch": "taille différente sur Drive",
    "missing": "absent de Drive",
    "notUploaded": "jamais envoyé sur Drive"
  }
}
```

`apps/web/public/locales/en/backups.json`:
```json
{
  "statusFilters": {
    "all": "All",
    "success": "Success",
    "failed": "Failed",
    "running": "Running"
  },
  "localStorage": "Local storage",
  "newBackup": "New backup",
  "cancel": "Cancel",
  "newJob": "New job",
  "loading": "Loading…",
  "noJobConfigured": "No job configured.",
  "targetsLabel": "targets: {{targets}}",
  "deleteJobConfirm": {
    "title": "Delete job {{name}}?",
    "confirmLabel": "Delete"
  },
  "detectedDbs": {
    "title": "Partial backup — detected databases",
    "explanation": "A database dump is fast and behaves like a \"partial\" backup — unlike a full volume/folder snapshot.",
    "locationContainer": "container",
    "locationHost": "host",
    "dump": "Dump",
    "dumpAndDownloadTitle": "Dump then download",
    "databasesLabel": "Databases: {{list}}",
    "noAppDatabases": "No application database (excluding system databases)",
    "none": "No database detected."
  },
  "history": {
    "title": "History",
    "restorePrompt": "To restore a backup, go to",
    "restoreLink": "Restore",
    "empty": "No history.",
    "downloadTitle": "Download",
    "deleteConfirm": {
      "title": "Delete this backup?",
      "description": "The local file will be deleted (existing USB/Drive copies are not affected). Irreversible action.",
      "confirmLabel": "Delete"
    }
  },
  "newJobForm": {
    "step1Title": "Step 1 — Type",
    "kindFull": "Full",
    "kindPartial": "Partial (database)",
    "sourceTypeVolume": "Docker volume",
    "sourceTypePath": "folder (bind mount)",
    "jobNamePlaceholder": "Job name",
    "loadVolumesError": "Unable to load the volume list",
    "loadDbsError": "Unable to load the database list",
    "loadMountsError": "Unable to load the mounted folder list",
    "sourceRefVolumePlaceholder": "Source reference (volume name)",
    "sourceRefPathPlaceholder": "Absolute path (e.g. /opt/docker-data/my-app)",
    "chooseFromList": "Choose from the list",
    "selectVolumePrompt": "Select a volume…",
    "selectMountPrompt": "Select a mounted folder…",
    "enterManually": "Enter manually",
    "partialExplanation": "For a one-off dump, use the \"Dump\" button in the \"Detected databases\" card below. This form creates a schedulable job — the source is a detected database.",
    "sourceRefDbPlaceholder": "Source reference (location:ref, e.g. native:mariadb)",
    "selectDbPrompt": "Select a database…",
    "step2Title": "Step 2 — Destination",
    "targetLocal": "local",
    "targetGdrive": "gdrive",
    "targetGdriveNotConnected": "(not connected)",
    "targetUsb": "usb",
    "targetUsbNotConfigured": "(not configured — see below)",
    "downloadAfter": "download after backup",
    "cronPlaceholder": "Cron expression (optional)",
    "validationError": "Name, reference, and at least one target are required",
    "creating": "Creating…",
    "create": "Create"
  },
  "driveStatus": {
    "verified": "verified on Drive",
    "sizeMismatch": "different size on Drive",
    "missing": "missing from Drive",
    "notUploaded": "never sent to Drive"
  }
}
```

`apps/web/public/locales/ta/backups.json`:
```json
{
  "statusFilters": {
    "all": "அனைத்தும்",
    "success": "வெற்றி",
    "failed": "தோல்வி",
    "running": "நடந்துகொண்டிருக்கிறது"
  },
  "localStorage": "உள்ளூர் சேமிப்பு",
  "newBackup": "புதிய காப்புப்பிரதி",
  "cancel": "ரத்துசெய்",
  "newJob": "புதிய job",
  "loading": "ஏற்றுகிறது…",
  "noJobConfigured": "job எதுவும் கட்டமைக்கப்படவில்லை.",
  "targetsLabel": "இலக்குகள்: {{targets}}",
  "deleteJobConfirm": {
    "title": "{{name}} job ஐ நீக்கவா?",
    "confirmLabel": "நீக்கு"
  },
  "detectedDbs": {
    "title": "பகுதி காப்புப்பிரதி — கண்டறியப்பட்ட தரவுத்தளங்கள்",
    "explanation": "தரவுத்தள dump வேகமானது மற்றும் « பகுதி » காப்புப்பிரதி போல செயல்படுகிறது — முழுமையான தொகுதி/கோப்புறை ஸ்னாப்ஷாட்டைப் போலல்லாமல்.",
    "locationContainer": "கொள்கலன்",
    "locationHost": "ஹோஸ்ட்",
    "dump": "Dump",
    "dumpAndDownloadTitle": "Dump செய்து பதிவிறக்கு",
    "databasesLabel": "தரவுத்தளங்கள்: {{list}}",
    "noAppDatabases": "பயன்பாட்டு தரவுத்தளம் இல்லை (System தரவுத்தளங்களைத் தவிர்த்து)",
    "none": "தரவுத்தளம் எதுவும் கண்டறியப்படவில்லை."
  },
  "history": {
    "title": "வரலாறு",
    "restorePrompt": "காப்புப்பிரதியை மீட்டமைக்க, இங்கு செல்லவும்",
    "restoreLink": "Restore",
    "empty": "வரலாறு இல்லை.",
    "downloadTitle": "பதிவிறக்கு",
    "deleteConfirm": {
      "title": "இந்த காப்புப்பிரதியை நீக்கவா?",
      "description": "உள்ளூர் கோப்பு நீக்கப்படும் (ஏற்கனவே உள்ள USB/Drive நகல்கள் பாதிக்கப்படாது). மீளமுடியாத செயல்.",
      "confirmLabel": "நீக்கு"
    }
  },
  "newJobForm": {
    "step1Title": "படி 1 — வகை",
    "kindFull": "முழுமையான",
    "kindPartial": "பகுதி (தரவுத்தளம்)",
    "sourceTypeVolume": "Docker தொகுதி",
    "sourceTypePath": "கோப்புறை (bind mount)",
    "jobNamePlaceholder": "job பெயர்",
    "loadVolumesError": "தொகுதி பட்டியலை ஏற்ற முடியவில்லை",
    "loadDbsError": "தரவுத்தள பட்டியலை ஏற்ற முடியவில்லை",
    "loadMountsError": "ஏற்றப்பட்ட கோப்புறை பட்டியலை ஏற்ற முடியவில்லை",
    "sourceRefVolumePlaceholder": "மூல குறிப்பு (தொகுதி பெயர்)",
    "sourceRefPathPlaceholder": "முழுமையான பாதை (எ.கா: /opt/docker-data/my-app)",
    "chooseFromList": "பட்டியலிலிருந்து தேர்ந்தெடு",
    "selectVolumePrompt": "ஒரு தொகுதியைத் தேர்ந்தெடு…",
    "selectMountPrompt": "ஏற்றப்பட்ட கோப்புறையைத் தேர்ந்தெடு…",
    "enterManually": "கைமுறையாக உள்ளிடு",
    "partialExplanation": "ஒரு முறை dump செய்ய, கீழே உள்ள « கண்டறியப்பட்ட தரவுத்தளங்கள் » அட்டையில் « Dump » பொத்தானைப் பயன்படுத்தவும். இந்த படிவம் திட்டமிடக்கூடிய job ஐ உருவாக்குகிறது — மூலம் ஒரு கண்டறியப்பட்ட தரவுத்தளம்.",
    "sourceRefDbPlaceholder": "மூல குறிப்பு (location:ref, எ.கா: native:mariadb)",
    "selectDbPrompt": "ஒரு தரவுத்தளத்தைத் தேர்ந்தெடு…",
    "step2Title": "படி 2 — இலக்கு",
    "targetLocal": "local",
    "targetGdrive": "gdrive",
    "targetGdriveNotConnected": "(இணைக்கப்படவில்லை)",
    "targetUsb": "usb",
    "targetUsbNotConfigured": "(கட்டமைக்கப்படவில்லை — கீழே பார்க்கவும்)",
    "downloadAfter": "காப்புப்பிரதிக்குப் பிறகு பதிவிறக்கு",
    "cronPlaceholder": "Cron வெளிப்பாடு (விருப்பத்தேர்வு)",
    "validationError": "பெயர், குறிப்பு, மற்றும் குறைந்தது ஒரு இலக்கு தேவை",
    "creating": "உருவாக்குகிறது…",
    "create": "உருவாக்கு"
  },
  "driveStatus": {
    "verified": "Drive இல் சரிபார்க்கப்பட்டது",
    "sizeMismatch": "Drive இல் வேறு அளவு",
    "missing": "Drive இல் இல்லை",
    "notUploaded": "Drive க்கு ஒருபோதும் அனுப்பப்படவில்லை"
  }
}
```

- [ ] **Step 2: Traduire les composants ciblés dans `Backups.tsx` (lignes 1-710)**

Remplacer les lignes 1 à 710 du fichier `apps/web/src/routes/Backups.tsx` (tout ce qui précède le commentaire JSDoc de `GDriveCompareCard`) par :
```tsx
import { useEffect, useMemo, useState } from "react";
import { Link } from "react-router-dom";
import type {
  BackupJob,
  BackupHistoryEntry,
  BackupRunStatus,
  BackupSourceType,
  BackupTarget,
  DetectedDatabase,
  DetectedBindMount,
  VolumeSummary,
  GDriveComparisonResult,
  UsbStatus,
  UsbBackupArchive,
} from "@pwa-admin/shared";
import { useTranslation } from "react-i18next";
import { apiJson } from "@/lib/api";
import { Card, CardTitle } from "@/components/ui/Card";
import { Button } from "@/components/ui/Button";
import { ConfirmDialog } from "@/components/ui/ConfirmDialog";
import { formatBytes } from "./Docker";
import { triggerBackupDownload, pollBackupRun } from "@/lib/pollBackupRun";
import { useMigrationSnapshot } from "@/lib/useMigrationSnapshot";
import {
  Play,
  Trash2,
  Database,
  HardDrive,
  Cloud,
  CheckCircle2,
  XCircle,
  AlertTriangle,
  Search,
  Loader2,
  Usb,
  Download,
  ArrowRight,
  HardDriveUpload,
} from "lucide-react";

const STATUS_FILTERS: { key: "all" | BackupRunStatus; labelKey: string }[] = [
  { key: "all", labelKey: "statusFilters.all" },
  { key: "success", labelKey: "statusFilters.success" },
  { key: "failed", labelKey: "statusFilters.failed" },
  { key: "running", labelKey: "statusFilters.running" },
];

function historyCardClass(status: BackupRunStatus): string | undefined {
  if (status === "success") return "border-primary/40 bg-primary/5";
  if (status === "failed") return "border-destructive/50 bg-destructive/5";
  return undefined; // pending/running — no verdict yet
}

export function Backups() {
  const { t } = useTranslation("backups");
  const [jobs, setJobs] = useState<BackupJob[] | null>(null);
  const [history, setHistory] = useState<BackupHistoryEntry[] | null>(null);
  const [statusFilter, setStatusFilter] = useState<"all" | BackupRunStatus>("all");
  const [storage, setStorage] = useState<{ localUsedBytes: number } | null>(null);
  const [detected, setDetected] = useState<DetectedDatabase[] | null>(null);
  const [showNewJob, setShowNewJob] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [gdriveAuthorized, setGdriveAuthorized] = useState(false);
  const [comparison, setComparison] = useState<GDriveComparisonResult | null>(null);
  const [comparing, setComparing] = useState(false);
  const [usbAvailable, setUsbAvailable] = useState(false);
  const [dumpingRef, setDumpingRef] = useState<string | null>(null);
  const [deletingRunId, setDeletingRunId] = useState<string | null>(null);

  async function runComparison() {
    setComparing(true);
    setError(null);
    try {
      setComparison(await apiJson<GDriveComparisonResult>("/backups/gdrive/compare"));
    } catch (err) {
      setError((err as Error).message);
    } finally {
      setComparing(false);
    }
  }

  const driveStatusByRunId = useMemo(() => {
    const map = new Map<string, GDriveComparisonResult["verifications"][number]["status"]>();
    comparison?.verifications.forEach((v) => map.set(v.runId, v.status));
    return map;
  }, [comparison]);

  async function loadJobs() {
    setJobs(await apiJson<BackupJob[]>("/backups/jobs"));
  }
  async function loadHistory() {
    setHistory(await apiJson<BackupHistoryEntry[]>("/backups/history?limit=100&offset=0"));
  }

  useEffect(() => {
    loadJobs().catch((err) => setError((err as Error).message));
    loadHistory().catch((err) => setError((err as Error).message));
    apiJson<{ localUsedBytes: number }>("/backups/storage")
      .then(setStorage)
      .catch(() => {});
    apiJson<DetectedDatabase[]>("/dbbackup/detect")
      .then(setDetected)
      .catch(() => setDetected([]));
    apiJson<{ authorized: boolean }>("/backups/gdrive/status")
      .then((s) => setGdriveAuthorized(s.authorized))
      .catch(() => setGdriveAuthorized(false));
    apiJson<UsbStatus>("/backups/usb/status")
      .then((s) => setUsbAvailable(s.drives.some((d) => d.isBackupConfigured)))
      .catch(() => setUsbAvailable(false));
  }, []);

  async function runJob(id: number) {
    await apiJson(`/backups/jobs/${id}/run`, { method: "POST" });
    await loadHistory();
  }

  async function deleteJob(id: number) {
    await apiJson(`/backups/jobs/${id}`, { method: "DELETE" });
    await loadJobs();
  }

  async function dumpDb(location: "docker" | "native", ref: string, download: boolean) {
    setDumpingRef(ref);
    setError(null);
    try {
      const targets: BackupTarget[] = ["local"];
      if (gdriveAuthorized) targets.push("gdrive");
      if (usbAvailable) targets.push("usb");
      const { runId } = await apiJson<{ runId: string }>(
        `/dbbackup/${location}/${encodeURIComponent(ref)}/dump`,
        { method: "POST", body: JSON.stringify({ targets }) }
      );
      await loadHistory();
      await pollBackupRun(runId, download);
      await loadHistory();
    } catch (err) {
      setError((err as Error).message);
    } finally {
      setDumpingRef(null);
    }
  }

  async function deleteRun(runId: string) {
    setDeletingRunId(runId);
    try {
      await apiJson(`/backups/history/${runId}`, { method: "DELETE" });
      await loadHistory();
    } catch (err) {
      setError((err as Error).message);
    } finally {
      setDeletingRunId(null);
    }
  }

  const filteredHistory = useMemo(() => {
    if (!history) return [];
    if (statusFilter === "all") return history;
    return history.filter((h) => h.status === statusFilter);
  }, [history, statusFilter]);

  return (
    <div className="flex flex-col gap-4">
      {error && <Card className="text-sm text-destructive">{error}</Card>}

      <Card>
        <CardTitle className="flex items-center gap-1">
          <HardDrive className="h-4 w-4" /> {t("localStorage")}
        </CardTitle>
        <p className="text-lg font-medium">{storage ? formatBytes(storage.localUsedBytes) : "…"}</p>
      </Card>

      <GDriveCompareCard
        comparison={comparison}
        comparing={comparing}
        onCompare={runComparison}
        onDeletedFile={runComparison}
      />

      <UsbConnection />

      <MigrationSnapshotCard />

      <div>
        <div className="mb-2 flex items-center justify-between">
          <h2 className="text-sm font-semibold text-muted-foreground">{t("newBackup")}</h2>
          <Button size="sm" variant="outline" onClick={() => setShowNewJob((v) => !v)}>
            {showNewJob ? t("cancel") : t("newJob")}
          </Button>
        </div>

        {showNewJob && (
          <NewJobForm
            onCreated={() => {
              setShowNewJob(false);
              loadJobs();
            }}
          />
        )}

        <div className="flex flex-col gap-3">
          {!jobs && <Card className="text-sm text-muted-foreground">{t("loading")}</Card>}
          {jobs?.length === 0 && <Card className="text-sm text-muted-foreground">{t("noJobConfigured")}</Card>}
          {jobs?.map((job) => (
            <Card key={job.id}>
              <div className="flex items-start justify-between gap-2">
                <div className="min-w-0">
                  <p className="truncate font-medium">{job.name}</p>
                  <p className="text-xs text-muted-foreground">
                    {job.sourceType} · {job.sourceRef}
                  </p>
                  <p className="text-xs text-muted-foreground">
                    {t("targetsLabel", { targets: job.targets.join(", ") })}
                    {job.scheduleCron ? ` · cron: ${job.scheduleCron}` : ""}
                  </p>
                </div>
                <div className="flex shrink-0 gap-2">
                  <Button size="sm" variant="outline" onClick={() => runJob(job.id)}>
                    <Play className="h-3.5 w-3.5" />
                  </Button>
                  <ConfirmDialog
                    trigger={
                      <Button size="sm" variant="destructive">
                        <Trash2 className="h-3.5 w-3.5" />
                      </Button>
                    }
                    title={t("deleteJobConfirm.title", { name: job.name })}
                    confirmLabel={t("deleteJobConfirm.confirmLabel")}
                    onConfirm={() => deleteJob(job.id)}
                  />
                </div>
              </div>
            </Card>
          ))}
        </div>
      </div>

      <Card>
        <CardTitle className="flex items-center gap-1">
          <Database className="h-4 w-4" /> {t("detectedDbs.title")}
        </CardTitle>
        <p className="text-xs text-muted-foreground">{t("detectedDbs.explanation")}</p>
        {detected && detected.length > 0 ? (
          <div className="mt-2 flex flex-col gap-3">
            {detected.map((d) => (
              <div key={`${d.location}:${d.ref}`} className="flex flex-col gap-1">
                <div className="flex items-center justify-between text-sm">
                  <span>
                    {d.displayName}{" "}
                    <span className="text-xs text-muted-foreground">
                      ({d.engine} · {d.location === "docker" ? t("detectedDbs.locationContainer") : t("detectedDbs.locationHost")})
                    </span>
                  </span>
                  <div className="flex gap-1">
                    <Button
                      size="sm"
                      variant="outline"
                      disabled={dumpingRef === d.ref}
                      onClick={() => dumpDb(d.location, d.ref, false)}
                    >
                      {dumpingRef === d.ref ? <Loader2 className="h-3.5 w-3.5 animate-spin" /> : t("detectedDbs.dump")}
                    </Button>
                    <Button
                      size="sm"
                      variant="outline"
                      disabled={dumpingRef === d.ref}
                      onClick={() => dumpDb(d.location, d.ref, true)}
                      title={t("detectedDbs.dumpAndDownloadTitle")}
                    >
                      <Download className="h-3.5 w-3.5" />
                    </Button>
                  </div>
                </div>
                {d.databases && d.databases.length > 0 && (
                  <p className="text-xs text-muted-foreground">{t("detectedDbs.databasesLabel", { list: d.databases.join(", ") })}</p>
                )}
                {d.databases && d.databases.length === 0 && (
                  <p className="text-xs text-muted-foreground">{t("detectedDbs.noAppDatabases")}</p>
                )}
              </div>
            ))}
          </div>
        ) : (
          <p className="mt-2 text-sm text-muted-foreground">{t("detectedDbs.none")}</p>
        )}
      </Card>

      <div>
        <div className="mb-2 flex items-center justify-between">
          <h2 className="text-sm font-semibold text-muted-foreground">{t("history.title")}</h2>
          <select
            value={statusFilter}
            onChange={(e) => setStatusFilter(e.target.value as "all" | BackupRunStatus)}
            className="rounded-md border border-border bg-background px-2 py-1 text-xs outline-none"
          >
            {STATUS_FILTERS.map((f) => (
              <option key={f.key} value={f.key}>
                {t(f.labelKey)}
              </option>
            ))}
          </select>
        </div>

        <p className="mb-2 flex items-center gap-1 text-xs text-muted-foreground">
          {t("history.restorePrompt")}{" "}
          <Link to="/restore" className="inline-flex items-center gap-0.5 text-primary underline">
            {t("history.restoreLink")} <ArrowRight className="h-3 w-3" />
          </Link>
        </p>

        <div className="flex flex-col gap-2">
          {filteredHistory.length === 0 && (
            <Card className="text-sm text-muted-foreground">{t("history.empty")}</Card>
          )}
          {filteredHistory.map((h) => {
            const driveStatus = driveStatusByRunId.get(h.runId);
            return (
              <Card key={h.runId} className={historyCardClass(h.status)}>
                <div className="flex items-center justify-between text-sm">
                  <div className="min-w-0">
                    <p className="truncate font-medium">
                      {h.type} · {h.sourceType}:{h.sourceRef}
                    </p>
                    <p className="text-xs text-muted-foreground">
                      {h.target}
                      {h.driveFileId ? " + gdrive" : ""}
                      {h.usbPath ? " + usb" : ""} · {new Date(h.startedAt).toLocaleString()}
                      {h.durationMs != null ? ` · ${(h.durationMs / 1000).toFixed(1)}s` : ""}
                    </p>
                  </div>
                  <span
                    className={
                      "shrink-0 rounded-full px-2 py-0.5 text-xs font-medium " +
                      (h.status === "success"
                        ? "bg-primary/15 text-primary"
                        : h.status === "failed"
                          ? "bg-destructive/15 text-destructive"
                          : "bg-warning/15 text-warning")
                    }
                  >
                    {h.status}
                  </span>
                </div>
                <div className="mt-1 flex items-center justify-between">
                  <div className="flex items-center gap-2">
                    {h.sizeBytes != null && (
                      <p className="text-xs text-muted-foreground">{formatBytes(h.sizeBytes)}</p>
                    )}
                    {driveStatus && <DriveStatusBadge status={driveStatus} />}
                  </div>
                  <div className="flex gap-1">
                    {h.status === "success" && h.target === "local" && (
                      <Button size="sm" variant="outline" onClick={() => triggerBackupDownload(h.runId)} title={t("history.downloadTitle")}>
                        <Download className="h-3.5 w-3.5" />
                      </Button>
                    )}
                    <ConfirmDialog
                      trigger={
                        <Button size="sm" variant="destructive" disabled={deletingRunId === h.runId}>
                          <Trash2 className="h-3.5 w-3.5" />
                        </Button>
                      }
                      title={t("history.deleteConfirm.title")}
                      description={t("history.deleteConfirm.description")}
                      confirmLabel={t("history.deleteConfirm.confirmLabel")}
                      onConfirm={() => deleteRun(h.runId)}
                    />
                  </div>
                </div>
              </Card>
            );
          })}
        </div>
      </div>
    </div>
  );
}

function NewJobForm({ onCreated }: { onCreated: () => void }) {
  const { t } = useTranslation("backups");
  const [step, setStep] = useState<1 | 2>(1);
  const [kind, setKind] = useState<"complet" | "partiel">("complet");
  const [name, setName] = useState("");
  const [sourceType, setSourceType] = useState<BackupSourceType>("volume");
  const [sourceRef, setSourceRef] = useState("");
  const [targets, setTargets] = useState<BackupTarget[]>(["local"]);
  const [downloadAfter, setDownloadAfter] = useState(false);
  const [cron, setCron] = useState("");
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const [volumes, setVolumes] = useState<VolumeSummary[] | null>(null);
  const [detectedDbs, setDetectedDbs] = useState<DetectedDatabase[] | null>(null);
  const [bindMounts, setBindMounts] = useState<DetectedBindMount[] | null>(null);
  const [gdriveAuthorized, setGdriveAuthorized] = useState(false);
  const [usbAvailable, setUsbAvailable] = useState(false);
  const [refsLoading, setRefsLoading] = useState(false);
  const [manualEntry, setManualEntry] = useState(false);

  useEffect(() => {
    apiJson<{ authorized: boolean }>("/backups/gdrive/status")
      .then((s) => setGdriveAuthorized(s.authorized))
      .catch(() => setGdriveAuthorized(false));
    apiJson<UsbStatus>("/backups/usb/status")
      .then((s) => setUsbAvailable(s.drives.some((d) => d.isBackupConfigured)))
      .catch(() => setUsbAvailable(false));
  }, []);

  useEffect(() => {
    if (kind === "partiel") {
      setSourceType("db");
      return;
    }
    setSourceType((prev) => (prev === "db" ? "volume" : prev));
  }, [kind]);

  useEffect(() => {
    setSourceRef("");
    setManualEntry(false);
    setRefsLoading(true);

    if (sourceType === "volume") {
      apiJson<VolumeSummary[]>("/docker/volumes")
        .then(setVolumes)
        .catch(() => setError(t("newJobForm.loadVolumesError")))
        .finally(() => setRefsLoading(false));
    } else if (sourceType === "db") {
      apiJson<DetectedDatabase[]>("/dbbackup/detect")
        .then(setDetectedDbs)
        .catch(() => setError(t("newJobForm.loadDbsError")))
        .finally(() => setRefsLoading(false));
    } else if (sourceType === "path") {
      apiJson<DetectedBindMount[]>("/backups/bind-mounts")
        .then(setBindMounts)
        .catch(() => setError(t("newJobForm.loadMountsError")))
        .finally(() => setRefsLoading(false));
    }
  }, [sourceType]);

  const refOptions: { value: string; label: string }[] =
    sourceType === "volume"
      ? (volumes ?? []).map((v) => ({ value: v.name, label: v.name }))
      : sourceType === "db"
        ? (detectedDbs ?? []).map((d) => ({
            value: `${d.location}:${d.ref}`,
            label: `${d.displayName} (${d.engine})`,
          }))
        : sourceType === "path"
          ? (bindMounts ?? []).map((m) => ({
              value: m.hostPath,
              label: `${m.hostPath} (${m.containerName})`,
            }))
          : [];

  function toggleTarget(tgt: BackupTarget) {
    setTargets((prev) => (prev.includes(tgt) ? prev.filter((x) => x !== tgt) : [...prev, tgt]));
  }

  async function submit(e: React.FormEvent) {
    e.preventDefault();
    setError(null);
    if (!name.trim() || !sourceRef.trim() || targets.length === 0) {
      setError(t("newJobForm.validationError"));
      return;
    }
    setSubmitting(true);
    try {
      const job = await apiJson<{ id: number }>("/backups/jobs", {
        method: "POST",
        body: JSON.stringify({
          name: name.trim(),
          sourceType,
          sourceRef: sourceRef.trim(),
          targets,
          scheduleCron: cron.trim() || undefined,
        }),
      });
      if (downloadAfter) {
        const { runId } = await apiJson<{ runId: string }>(`/backups/jobs/${job.id}/run`, { method: "POST" });
        pollBackupRun(runId, true).catch(() => {});
      }
      onCreated();
    } catch (err) {
      setError((err as Error).message);
    } finally {
      setSubmitting(false);
    }
  }

  return (
    <Card className="mb-3">
      <form onSubmit={submit} className="flex flex-col gap-3">
        <div>
          <p className="mb-1 text-xs font-medium text-muted-foreground">{t("newJobForm.step1Title")}</p>
          <div className="flex overflow-hidden rounded-md border border-border text-sm">
            <button
              type="button"
              onClick={() => {
                setKind("complet");
                setStep(1);
              }}
              className={`flex-1 px-3 py-2 ${kind === "complet" ? "bg-primary text-primary-foreground" : "bg-background"}`}
            >
              {t("newJobForm.kindFull")}
            </button>
            <button
              type="button"
              onClick={() => {
                setKind("partiel");
                setStep(1);
              }}
              className={`flex-1 px-3 py-2 ${kind === "partiel" ? "bg-primary text-primary-foreground" : "bg-background"}`}
            >
              {t("newJobForm.kindPartial")}
            </button>
          </div>
        </div>

        {kind === "complet" ? (
          <>
            <select
              value={sourceType}
              onChange={(e) => setSourceType(e.target.value as BackupSourceType)}
              className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm outline-none"
            >
              <option value="volume">{t("newJobForm.sourceTypeVolume")}</option>
              <option value="path">{t("newJobForm.sourceTypePath")}</option>
            </select>
            <input
              type="text"
              placeholder={t("newJobForm.jobNamePlaceholder")}
              value={name}
              onChange={(e) => setName(e.target.value)}
              className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-primary"
            />
            {manualEntry || refOptions.length === 0 ? (
              <div className="flex flex-col gap-1">
                <input
                  type="text"
                  placeholder={
                    refsLoading
                      ? t("newJobForm.loading", { defaultValue: t("loading") })
                      : sourceType === "volume"
                        ? t("newJobForm.sourceRefVolumePlaceholder")
                        : t("newJobForm.sourceRefPathPlaceholder")
                  }
                  value={sourceRef}
                  onChange={(e) => setSourceRef(e.target.value)}
                  className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-primary"
                />
                {refOptions.length > 0 && (
                  <button
                    type="button"
                    onClick={() => setManualEntry(false)}
                    className="self-start text-xs text-primary underline"
                  >
                    {t("newJobForm.chooseFromList")}
                  </button>
                )}
              </div>
            ) : (
              <div className="flex flex-col gap-1">
                <select
                  value={sourceRef}
                  onChange={(e) => setSourceRef(e.target.value)}
                  className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm outline-none"
                >
                  <option value="">
                    {sourceType === "volume" ? t("newJobForm.selectVolumePrompt") : t("newJobForm.selectMountPrompt")}
                  </option>
                  {refOptions.map((opt) => (
                    <option key={opt.value} value={opt.value}>
                      {opt.label}
                    </option>
                  ))}
                </select>
                <button
                  type="button"
                  onClick={() => setManualEntry(true)}
                  className="self-start text-xs text-primary underline"
                >
                  {t("newJobForm.enterManually")}
                </button>
              </div>
            )}
          </>
        ) : (
          <p className="text-xs text-muted-foreground">{t("newJobForm.partialExplanation")}</p>
        )}

        {kind === "partiel" && (
          <>
            <input
              type="text"
              placeholder={t("newJobForm.jobNamePlaceholder")}
              value={name}
              onChange={(e) => setName(e.target.value)}
              className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-primary"
            />
            {manualEntry || refOptions.length === 0 ? (
              <div className="flex flex-col gap-1">
                <input
                  type="text"
                  placeholder={refsLoading ? t("loading") : t("newJobForm.sourceRefDbPlaceholder")}
                  value={sourceRef}
                  onChange={(e) => setSourceRef(e.target.value)}
                  className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-primary"
                />
                {refOptions.length > 0 && (
                  <button
                    type="button"
                    onClick={() => setManualEntry(false)}
                    className="self-start text-xs text-primary underline"
                  >
                    {t("newJobForm.chooseFromList")}
                  </button>
                )}
              </div>
            ) : (
              <div className="flex flex-col gap-1">
                <select
                  value={sourceRef}
                  onChange={(e) => setSourceRef(e.target.value)}
                  className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm outline-none"
                >
                  <option value="">{t("newJobForm.selectDbPrompt")}</option>
                  {refOptions.map((opt) => (
                    <option key={opt.value} value={opt.value}>
                      {opt.label}
                    </option>
                  ))}
                </select>
                <button
                  type="button"
                  onClick={() => setManualEntry(true)}
                  className="self-start text-xs text-primary underline"
                >
                  {t("newJobForm.enterManually")}
                </button>
              </div>
            )}
          </>
        )}

        <div>
          <p className="mb-1 text-xs font-medium text-muted-foreground">{t("newJobForm.step2Title")}</p>
          <div className="flex flex-wrap gap-3 text-sm">
            <label className="flex items-center gap-1">
              <input type="checkbox" checked={targets.includes("local")} onChange={() => toggleTarget("local")} />
              {t("newJobForm.targetLocal")}
            </label>
            <label className="flex items-center gap-1">
              <input
                type="checkbox"
                checked={targets.includes("gdrive")}
                onChange={() => toggleTarget("gdrive")}
                disabled={!gdriveAuthorized}
              />
              {t("newJobForm.targetGdrive")}
              {!gdriveAuthorized && <span className="text-xs text-muted-foreground">{t("newJobForm.targetGdriveNotConnected")}</span>}
            </label>
            <label className="flex items-center gap-1">
              <input
                type="checkbox"
                checked={targets.includes("usb")}
                onChange={() => toggleTarget("usb")}
                disabled={!usbAvailable}
              />
              {t("newJobForm.targetUsb")}
              {!usbAvailable && <span className="text-xs text-muted-foreground">{t("newJobForm.targetUsbNotConfigured")}</span>}
            </label>
            <label className="flex items-center gap-1">
              <input type="checkbox" checked={downloadAfter} onChange={() => setDownloadAfter((v) => !v)} />
              {t("newJobForm.downloadAfter")}
            </label>
          </div>
        </div>

        <input
          type="text"
          placeholder={t("newJobForm.cronPlaceholder")}
          value={cron}
          onChange={(e) => setCron(e.target.value)}
          className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-primary"
        />
        {error && <p className="text-xs text-destructive">{error}</p>}
        <Button type="submit" size="sm" disabled={submitting}>
          {submitting ? t("newJobForm.creating") : t("newJobForm.create")}
        </Button>
      </form>
    </Card>
  );
}

function DriveStatusBadge({ status }: { status: "verified" | "missing" | "size-mismatch" | "not-uploaded" }) {
  const { t } = useTranslation("backups");
  const config = {
    verified: { labelKey: "driveStatus.verified", cls: "bg-primary/15 text-primary", icon: CheckCircle2 },
    "size-mismatch": { labelKey: "driveStatus.sizeMismatch", cls: "bg-warning/15 text-warning", icon: AlertTriangle },
    missing: { labelKey: "driveStatus.missing", cls: "bg-destructive/15 text-destructive", icon: XCircle },
    "not-uploaded": { labelKey: "driveStatus.notUploaded", cls: "bg-destructive/15 text-destructive", icon: XCircle },
  }[status];
  const Icon = config.icon;
  return (
    <span className={`flex items-center gap-1 rounded-full px-2 py-0.5 text-[10px] font-medium ${config.cls}`}>
      <Icon className="h-3 w-3" /> {t(config.labelKey)}
    </span>
  );
}

```

Notes importantes :
1. `toggleTarget(t: BackupTarget)` de l'original (l.454) est renommé en `toggleTarget(tgt: BackupTarget)` — même correction que dans les plans précédents (Wizard, Applications), pour éviter la collision avec la fonction `t()` du hook de traduction.
2. Un placeholder conditionnel utilisait `refsLoading ? "Chargement…" : ...` (l.541, 606) — traduit avec `t("newJobForm.loading", { defaultValue: t("loading") })` pour le premier cas (avec fallback explicite vers la clé générique déjà connue) et simplement `t("loading")` pour le second. En pratique, il n'existe pas de clé `newJobForm.loading` dans le JSON créé à l'étape 1 — **utiliser directement `t("loading")` dans les deux cas** (la clé générique de haut niveau, déjà définie), pas de `defaultValue` imbriqué inutile. Corriger ainsi lors de l'implémentation :
```tsx
                  placeholder={
                    refsLoading
                      ? t("loading")
                      : sourceType === "volume"
                        ? t("newJobForm.sourceRefVolumePlaceholder")
                        : t("newJobForm.sourceRefPathPlaceholder")
                  }
```

- [ ] **Step 3: Lire l'état actuel de `lib/i18n.ts` et ajouter le namespace `backups`**

Lire `apps/web/src/lib/i18n.ts` pour connaître l'état exact de `ns: [...]` avant de modifier. Ajouter `"backups"` à la fin du tableau existant.

- [ ] **Step 4: Vérifier le typecheck**

Run: `npx tsc --noEmit -p apps/web/tsconfig.json`
Expected: aucune erreur (hors bruit préexistant `ImaNote.tsx`).

- [ ] **Step 5: Vérifier que le reste du fichier (GDriveCompareCard et suite) est bien intact**

Run: `grep -c "function GDriveCompareCard\|function UsbConnection\|function MigrationSnapshotCard" apps/web/src/routes/Backups.tsx`
Expected: `3` — confirme que ces fonctions existent toujours, non touchées par cette tâche.

- [ ] **Step 6: Commit**

```bash
git add apps/web/public/locales/fr/backups.json apps/web/public/locales/en/backups.json apps/web/public/locales/ta/backups.json apps/web/src/routes/Backups.tsx apps/web/src/lib/i18n.ts
git commit -m "feat(web): traduire l'écran Backups (jobs, historique) via i18next"
```

---

## Task 2: Traduire `GDriveCompareCard`, `GDriveCompareSummary`, `OrphanGroupCard`, `UsbConnection`, `MigrationSnapshotCard`

**Files:**
- Modify: `apps/web/public/locales/fr/backups.json` (ajout de clés, ne pas toucher aux clés existantes de Task 1)
- Modify: `apps/web/public/locales/en/backups.json` (idem)
- Modify: `apps/web/public/locales/ta/backups.json` (idem)
- Modify: `apps/web/src/routes/Backups.tsx` (lignes 712+ de l'original)

**Interfaces:**
- Consumes: `useTranslation("backups")` (namespace déjà enregistré dans `ns` par Task 1).
- Produces: rien de consommé par d'autres tâches (fin de l'écran Backups).

Avant de commencer, lire l'état réel des 3 fichiers `backups.json` créés/ajustés par Task 1.

Chaînes à extraire (voir contenu du fichier lu en entier plus haut, lignes 712-1120) :

**`GDriveCompareCard`** : "Google Drive" (l.743), "Non connecté — configurez la connexion dans"/"Settings" (l.746-747, avec lien).

**`GDriveCompareSummary`** : "Comparaison local ↔ Google Drive" (l.769), bouton "Vérification…"/"Vérifier Google Drive" (l.772), "Vérifiées sur Drive"/"Manquantes sur Drive"/"Fichiers Drive orphelins" (l.780, 784, 790), "Vérifié le {{date}} — le statut de chaque sauvegarde est affiché directement dans l'historique ci-dessous." (l.798-799, interpolé), "Présents sur Drive uniquement (pas de sauvegarde locale correspondante)" (l.805-806).

**`OrphanGroupCard`** : "({{count}} fichier(s))" (l.842, interpolé pluriel), titre+description confirmation suppression "Supprimer ce fichier sur Google Drive ?"/description/"Supprimer" (l.859-861).

**`UsbConnection`** : "Disque USB / SSD" (l.926), bouton "Rafraîchir" (l.929), "Disque démonté, débranchement possible en toute sécurité." (l.935), "Chargement…" (l.940), "Aucun disque USB détecté (branchez-le, il sera monté automatiquement)." (l.943), "{{free}} libre" avec suffixe total optionnel (l.954-955, interpolé), bouton "Éjection…"/"Éjecter" (l.961), titre+description confirmation éjection (l.964-966), "— non configuré comme disque de sauvegarde" (l.973), bouton "Activation…"/"Activer comme disque de sauvegarde" (l.986), bouton "Masquer les archives"/"Parcourir les archives sur le disque" (l.994), "Chargement…" (l.1000), "Aucune archive trouvée sur le disque." (l.1002).

**`MigrationSnapshotCard`** : "Migration serveur (disque externe)" (l.1051), texte explicatif long (l.1054-1058), "Aucun disque USB configuré comme sauvegarde (voir ci-dessus)." (l.1064-1065), checkbox "Inclure les conteneurs "-duplicate" (clones de bascule manuelle — généralement inutiles pour une migration vers un nouveau serveur)" (l.1076-1077), bouton "Capture en cours…"/"Prendre un instantané de migration" (l.1082-1086), "Site: {{name}}"/"Serveur complet" (l.1096, interpolé).

- [ ] **Step 1: Lire l'état actuel des 3 fichiers `backups.json` et y ajouter les clés restantes**

Ajouter dans `apps/web/public/locales/fr/backups.json`, après la dernière clé de haut niveau déjà présente (`"driveStatus"`, en ajoutant une virgule après cette clé) :
```json
  "gdrive": {
    "title": "Google Drive",
    "notConnected": "Non connecté — configurez la connexion dans",
    "settingsLink": "Settings",
    "compareTitle": "Comparaison local ↔ Google Drive",
    "verifying": "Vérification…",
    "verifyButton": "Vérifier Google Drive",
    "verifiedOnDrive": "Vérifiées sur Drive",
    "missingOnDrive": "Manquantes sur Drive",
    "orphanFiles": "Fichiers Drive orphelins",
    "checkedAt": "Vérifié le {{date}} — le statut de chaque sauvegarde est affiché directement dans l'historique ci-dessous.",
    "orphansOnly": "Présents sur Drive uniquement (pas de sauvegarde locale correspondante)",
    "fileCount_one": "({{count}} fichier)",
    "fileCount_other": "({{count}} fichiers)",
    "deleteFileConfirm": {
      "title": "Supprimer ce fichier sur Google Drive ?",
      "description": "Le fichier sera définitivement supprimé de Google Drive (aucune copie locale n'existe).",
      "confirmLabel": "Supprimer"
    }
  },
  "usb": {
    "title": "Disque USB / SSD",
    "refresh": "Rafraîchir",
    "ejectedSafe": "Disque démonté, débranchement possible en toute sécurité.",
    "loading": "Chargement…",
    "noneDetected": "Aucun disque USB détecté (branchez-le, il sera monté automatiquement).",
    "freeSpace": "{{free}} libre",
    "freeSpaceTotal": " / {{total}}",
    "ejecting": "Éjection…",
    "eject": "Éjecter",
    "ejectConfirm": {
      "title": "Éjecter ce disque ?",
      "description": "Le disque sera démonté proprement. Attendez la confirmation avant de le débrancher physiquement, sinon une sauvegarde en cours pourrait être corrompue.",
      "confirmLabel": "Éjecter"
    },
    "notConfigured": "— non configuré comme disque de sauvegarde",
    "activating": "Activation…",
    "activate": "Activer comme disque de sauvegarde",
    "hideArchives": "Masquer les archives",
    "browseArchives": "Parcourir les archives sur le disque",
    "noArchivesFound": "Aucune archive trouvée sur le disque."
  },
  "migration": {
    "title": "Migration serveur (disque externe)",
    "explanation": "Capture un instantané complet du serveur (images/volumes Docker, bases de données, config Nginx, applications, paquets système, configuration pwa-admin) sur le disque USB de sauvegarde — à utiliser en cas de changement de matériel. La restauration se fait depuis l'écran Restore, sur le nouveau serveur.",
    "noUsbConfigured": "Aucun disque USB configuré comme sauvegarde (voir ci-dessus).",
    "includeDuplicates": "Inclure les conteneurs \"-duplicate\" (clones de bascule manuelle — généralement inutiles pour une migration vers un nouveau serveur)",
    "capturing": "Capture en cours…",
    "startSnapshot": "Prendre un instantané de migration",
    "siteLabel": "Site: {{name}}",
    "fullServer": "Serveur complet"
  }
```

Ajouter dans `apps/web/public/locales/en/backups.json`, même position :
```json
  "gdrive": {
    "title": "Google Drive",
    "notConnected": "Not connected — set up the connection in",
    "settingsLink": "Settings",
    "compareTitle": "Local ↔ Google Drive comparison",
    "verifying": "Verifying…",
    "verifyButton": "Verify Google Drive",
    "verifiedOnDrive": "Verified on Drive",
    "missingOnDrive": "Missing from Drive",
    "orphanFiles": "Orphan Drive files",
    "checkedAt": "Checked on {{date}} — each backup's status is shown directly in the history below.",
    "orphansOnly": "Present on Drive only (no matching local backup)",
    "fileCount_one": "({{count}} file)",
    "fileCount_other": "({{count}} files)",
    "deleteFileConfirm": {
      "title": "Delete this file on Google Drive?",
      "description": "The file will be permanently deleted from Google Drive (no local copy exists).",
      "confirmLabel": "Delete"
    }
  },
  "usb": {
    "title": "USB / SSD Drive",
    "refresh": "Refresh",
    "ejectedSafe": "Drive unmounted, safe to unplug.",
    "loading": "Loading…",
    "noneDetected": "No USB drive detected (plug it in, it will be mounted automatically).",
    "freeSpace": "{{free}} free",
    "freeSpaceTotal": " / {{total}}",
    "ejecting": "Ejecting…",
    "eject": "Eject",
    "ejectConfirm": {
      "title": "Eject this drive?",
      "description": "The drive will be cleanly unmounted. Wait for confirmation before physically unplugging it, otherwise a backup in progress could be corrupted.",
      "confirmLabel": "Eject"
    },
    "notConfigured": "— not configured as a backup drive",
    "activating": "Activating…",
    "activate": "Set as backup drive",
    "hideArchives": "Hide archives",
    "browseArchives": "Browse archives on the drive",
    "noArchivesFound": "No archive found on the drive."
  },
  "migration": {
    "title": "Server migration (external drive)",
    "explanation": "Captures a full server snapshot (Docker images/volumes, databases, Nginx config, applications, system packages, pwa-admin configuration) to the backup USB drive — use in case of a hardware change. Restore is done from the Restore screen, on the new server.",
    "noUsbConfigured": "No USB drive configured as backup (see above).",
    "includeDuplicates": "Include \"-duplicate\" containers (manual failover clones — usually unnecessary for migrating to a new server)",
    "capturing": "Capturing…",
    "startSnapshot": "Take a migration snapshot",
    "siteLabel": "Site: {{name}}",
    "fullServer": "Full server"
  }
```

Ajouter dans `apps/web/public/locales/ta/backups.json`, même position :
```json
  "gdrive": {
    "title": "Google Drive",
    "notConnected": "இணைக்கப்படவில்லை — இணைப்பை இதில் அமைக்கவும்",
    "settingsLink": "Settings",
    "compareTitle": "உள்ளூர் ↔ Google Drive ஒப்பீடு",
    "verifying": "சரிபார்க்கிறது…",
    "verifyButton": "Google Drive ஐ சரிபார்",
    "verifiedOnDrive": "Drive இல் சரிபார்க்கப்பட்டது",
    "missingOnDrive": "Drive இல் இல்லை",
    "orphanFiles": "Drive அனாதை கோப்புகள்",
    "checkedAt": "{{date}} அன்று சரிபார்க்கப்பட்டது — ஒவ்வொரு காப்புப்பிரதியின் நிலையும் கீழே உள்ள வரலாற்றில் நேரடியாகக் காட்டப்படுகிறது.",
    "orphansOnly": "Drive இல் மட்டுமே உள்ளது (பொருந்தும் உள்ளூர் காப்புப்பிரதி இல்லை)",
    "fileCount_one": "({{count}} கோப்பு)",
    "fileCount_other": "({{count}} கோப்புகள்)",
    "deleteFileConfirm": {
      "title": "Google Drive இல் இந்த கோப்பை நீக்கவா?",
      "description": "கோப்பு Google Drive இலிருந்து நிரந்தரமாக நீக்கப்படும் (உள்ளூர் நகல் இல்லை).",
      "confirmLabel": "நீக்கு"
    }
  },
  "usb": {
    "title": "USB / SSD வட்டு",
    "refresh": "புதுப்பி",
    "ejectedSafe": "வட்டு அன்மவுன்ட் செய்யப்பட்டது, பாதுகாப்பாக அகற்றலாம்.",
    "loading": "ஏற்றுகிறது…",
    "noneDetected": "USB வட்டு எதுவும் கண்டறியப்படவில்லை (அதை இணைக்கவும், அது தானாக ஏற்றப்படும்).",
    "freeSpace": "{{free}} காலி",
    "freeSpaceTotal": " / {{total}}",
    "ejecting": "வெளியேற்றுகிறது…",
    "eject": "வெளியேற்று",
    "ejectConfirm": {
      "title": "இந்த வட்டை வெளியேற்றவா?",
      "description": "வட்டு சரியாக அன்மவுன்ட் செய்யப்படும். இயற்பியல் ரீதியாக அகற்றும் முன் உறுதிப்படுத்தலுக்காக காத்திருக்கவும், இல்லையெனில் நடந்துகொண்டிருக்கும் காப்புப்பிரதி சிதைந்துவிடலாம்.",
      "confirmLabel": "வெளியேற்று"
    },
    "notConfigured": "— காப்புப்பிரதி வட்டாக கட்டமைக்கப்படவில்லை",
    "activating": "செயல்படுத்துகிறது…",
    "activate": "காப்புப்பிரதி வட்டாக செயல்படுத்து",
    "hideArchives": "காப்பகங்களை மறை",
    "browseArchives": "வட்டில் உள்ள காப்பகங்களை உலாவு",
    "noArchivesFound": "வட்டில் காப்பகம் எதுவும் கிடைக்கவில்லை."
  },
  "migration": {
    "title": "சேவையக இடமாற்றம் (வெளிப்புற வட்டு)",
    "explanation": "சேவையகத்தின் முழுமையான ஸ்னாப்ஷாட்டை (Docker படங்கள்/தொகுதிகள், தரவுத்தளங்கள், Nginx கட்டமைப்பு, பயன்பாடுகள், System தொகுப்புகள், pwa-admin கட்டமைப்பு) காப்புப்பிரதி USB வட்டில் பிடிக்கிறது — வன்பொருள் மாற்றத்தின்போது பயன்படுத்தவும். மீட்டமைப்பு புதிய சேவையகத்தில் Restore திரையிலிருந்து செய்யப்படுகிறது.",
    "noUsbConfigured": "காப்புப்பிரதியாக எந்த USB வட்டும் கட்டமைக்கப்படவில்லை (மேலே பார்க்கவும்).",
    "includeDuplicates": "\"-duplicate\" கொள்கலன்களை சேர் (கைமுறை மாற்று நகல்கள் — புதிய சேவையகத்திற்கு இடமாற்றத்திற்கு பொதுவாக தேவையில்லை)",
    "capturing": "பிடிக்கிறது…",
    "startSnapshot": "இடமாற்ற ஸ்னாப்ஷாட்டை எடு",
    "siteLabel": "தளம்: {{name}}",
    "fullServer": "முழு சேவையகம்"
  }
```

- [ ] **Step 2: Traduire les composants restants**

Remplacer les lignes 712 à la fin du fichier (le commentaire JSDoc de `GDriveCompareCard` jusqu'à la fermeture finale de `MigrationSnapshotCard`) par :
```tsx
/**
 * Backup-context slice of what used to be GDriveConnection — connexion/
 * déconnexion du compte a déménagé vers Settings (réglages de compte), ce
 * qui reste ici est propre au contexte "sauvegardes" : comparer ce qui est
 * en local à ce qui est réellement sur Drive. Vérifie elle-même le statut
 * de connexion pour rester utilisable indépendamment de Settings — un
 * admin non connecté à Drive voit juste un message plutôt qu'un bouton
 * qui échouerait.
 */
function GDriveCompareCard({
  comparison,
  comparing,
  onCompare,
  onDeletedFile,
}: {
  comparison: GDriveComparisonResult | null;
  comparing: boolean;
  onCompare: () => void;
  onDeletedFile: () => void;
}) {
  const { t } = useTranslation("backups");
  const [authorized, setAuthorized] = useState<boolean | null>(null);

  useEffect(() => {
    apiJson<{ authorized: boolean }>("/backups/gdrive/status")
      .then((s) => setAuthorized(s.authorized))
      .catch(() => setAuthorized(false));
  }, []);

  return (
    <Card>
      <CardTitle className="flex items-center gap-1">
        <Cloud className="h-4 w-4" /> {t("gdrive.title")}
      </CardTitle>
      {authorized === false && (
        <p className="text-sm text-muted-foreground">
          {t("gdrive.notConnected")} <Link to="/settings" className="text-primary underline-offset-2 hover:underline">{t("gdrive.settingsLink")}</Link>.
        </p>
      )}
      {authorized && <GDriveCompareSummary comparison={comparison} comparing={comparing} onCompare={onCompare} onDeletedFile={onDeletedFile} />}
    </Card>
  );
}

function GDriveCompareSummary({
  comparison,
  comparing,
  onCompare,
  onDeletedFile,
}: {
  comparison: GDriveComparisonResult | null;
  comparing: boolean;
  onCompare: () => void;
  onDeletedFile: () => void;
}) {
  const { t } = useTranslation("backups");
  return (
    <div className="border-t border-border pt-3 first:border-t-0 first:pt-0">
      <div className="flex items-center justify-between">
        <p className="text-xs font-medium text-muted-foreground">{t("gdrive.compareTitle")}</p>
        <Button size="sm" variant="outline" onClick={onCompare} disabled={comparing}>
          {comparing ? <Loader2 className="h-3.5 w-3.5 animate-spin" /> : <Search className="h-3.5 w-3.5" />}
          {comparing ? t("gdrive.verifying") : t("gdrive.verifyButton")}
        </Button>
      </div>

      {comparison && (
        <div className="mt-3 flex flex-col gap-2">
          <div className="grid grid-cols-3 gap-2 text-xs">
            <div className="rounded-md border border-border p-2">
              <p className="text-muted-foreground">{t("gdrive.verifiedOnDrive")}</p>
              <p className="text-sm font-medium text-primary">{comparison.totalVerified}</p>
            </div>
            <div className="rounded-md border border-border p-2">
              <p className="text-muted-foreground">{t("gdrive.missingOnDrive")}</p>
              <p className={`text-sm font-medium ${comparison.totalMissing > 0 ? "text-destructive" : ""}`}>
                {comparison.totalMissing}
              </p>
            </div>
            <div className="rounded-md border border-border p-2">
              <p className="text-muted-foreground">{t("gdrive.orphanFiles")}</p>
              <p className={`text-sm font-medium ${comparison.totalOrphans > 0 ? "text-warning" : ""}`}>
                {comparison.totalOrphans}
              </p>
            </div>
          </div>

          <p className="text-xs text-muted-foreground">
            {t("gdrive.checkedAt", { date: new Date(comparison.checkedAt).toLocaleString() })}
          </p>

          {comparison.orphanGroups.length > 0 && (
            <div className="mt-1 flex flex-col gap-2">
              <p className="flex items-center gap-1 text-xs font-medium text-warning">
                <AlertTriangle className="h-3.5 w-3.5" /> {t("gdrive.orphansOnly")}
              </p>
              {comparison.orphanGroups.map((group) => (
                <OrphanGroupCard key={`${group.category}/${group.sourceRef}`} group={group} onDeleted={onDeletedFile} />
              ))}
            </div>
          )}
        </div>
      )}
    </div>
  );
}

function OrphanGroupCard({
  group,
  onDeleted,
}: {
  group: GDriveComparisonResult["orphanGroups"][number];
  onDeleted: () => void;
}) {
  const { t } = useTranslation("backups");
  const [deletingId, setDeletingId] = useState<string | null>(null);

  async function deleteFile(fileId: string) {
    setDeletingId(fileId);
    try {
      await apiJson(`/backups/gdrive/files/${fileId}`, { method: "DELETE" });
      onDeleted();
    } finally {
      setDeletingId(null);
    }
  }

  return (
    <div className="rounded-md border border-border p-2">
      <p className="text-xs font-medium">
        {group.category}/{group.sourceRef}
        <span className="ml-1 text-muted-foreground">{t("gdrive.fileCount", { count: group.files.length })}</span>
      </p>
      <div className="mt-1 flex flex-col gap-1">
        {group.files.map((f) => (
          <div key={f.fileId} className="flex items-center justify-between text-xs">
            <div className="min-w-0">
              <p className="truncate">{f.fileName}</p>
              <p className="text-muted-foreground">
                {formatBytes(f.sizeBytes)} · {new Date(f.modifiedAt).toLocaleDateString()}
              </p>
            </div>
            <ConfirmDialog
              trigger={
                <Button size="sm" variant="destructive" disabled={deletingId === f.fileId}>
                  <Trash2 className="h-3 w-3" />
                </Button>
              }
              title={t("gdrive.deleteFileConfirm.title")}
              description={t("gdrive.deleteFileConfirm.description")}
              confirmLabel={t("gdrive.deleteFileConfirm.confirmLabel")}
              onConfirm={() => deleteFile(f.fileId)}
            />
          </div>
        ))}
      </div>
    </div>
  );
}

function UsbConnection() {
  const { t } = useTranslation("backups");
  const [status, setStatus] = useState<UsbStatus | null>(null);
  const [archives, setArchives] = useState<UsbBackupArchive[] | null>(null);
  const [showArchives, setShowArchives] = useState(false);
  const [loadingArchives, setLoadingArchives] = useState(false);
  const [enabling, setEnabling] = useState<string | null>(null);
  const [ejecting, setEjecting] = useState<string | null>(null);
  const [ejected, setEjected] = useState<string | null>(null);

  function loadStatus() {
    apiJson<UsbStatus>("/backups/usb/status")
      .then(setStatus)
      .catch(() => setStatus(null));
  }

  useEffect(loadStatus, []);

  async function enableDrive(mountpoint: string) {
    setEnabling(mountpoint);
    try {
      await apiJson("/backups/usb/enable", { method: "POST", body: JSON.stringify({ mountpoint }) });
      loadStatus();
    } finally {
      setEnabling(null);
    }
  }

  async function ejectDrive(mountpoint: string) {
    setEjecting(mountpoint);
    try {
      await apiJson("/backups/usb/eject", { method: "POST", body: JSON.stringify({ mountpoint }) });
      setEjected(mountpoint);
      loadStatus();
    } finally {
      setEjecting(null);
    }
  }

  async function loadArchives() {
    setShowArchives((v) => !v);
    if (archives) return;
    setLoadingArchives(true);
    try {
      setArchives(await apiJson<UsbBackupArchive[]>("/backups/usb/archives"));
    } catch {
      setArchives([]);
    } finally {
      setLoadingArchives(false);
    }
  }

  return (
    <Card>
      <div className="flex items-center justify-between">
        <CardTitle className="flex items-center gap-1">
          <Usb className="h-4 w-4" /> {t("usb.title")}
        </CardTitle>
        <Button size="sm" variant="outline" onClick={loadStatus}>
          {t("usb.refresh")}
        </Button>
      </div>

      {ejected && (
        <p className="flex items-center gap-1 text-sm text-primary">
          <CheckCircle2 className="h-4 w-4" /> {t("usb.ejectedSafe")}
        </p>
      )}

      {!status ? (
        <p className="text-sm text-muted-foreground">{t("usb.loading")}</p>
      ) : !status.available ? (
        <p className="flex items-center gap-1 text-sm text-warning">
          <XCircle className="h-4 w-4" /> {t("usb.noneDetected")}
        </p>
      ) : (
        <div className="flex flex-col gap-2">
          {status.drives.map((d) =>
            d.isBackupConfigured ? (
              <div key={d.mountpoint} className="rounded-md border border-border p-2 text-sm">
                <p className="flex items-center gap-1 text-primary">
                  <CheckCircle2 className="h-4 w-4" /> {d.label} ({d.device})
                </p>
                <p className="text-xs text-muted-foreground">
                  {d.filesystem ?? "?"} · {t("usb.freeSpace", { free: d.freeBytes != null ? formatBytes(d.freeBytes) : "?" })}
                  {d.totalBytes != null ? t("usb.freeSpaceTotal", { total: formatBytes(d.totalBytes) }) : ""}
                </p>
                <p className="truncate text-xs text-muted-foreground">{d.backupRoot}</p>
                <ConfirmDialog
                  trigger={
                    <Button size="sm" variant="outline" className="mt-2" disabled={ejecting === d.mountpoint}>
                      {ejecting === d.mountpoint ? t("usb.ejecting") : t("usb.eject")}
                    </Button>
                  }
                  title={t("usb.ejectConfirm.title")}
                  description={t("usb.ejectConfirm.description")}
                  confirmLabel={t("usb.ejectConfirm.confirmLabel")}
                  onConfirm={() => ejectDrive(d.mountpoint)}
                />
              </div>
            ) : (
              <div key={d.mountpoint} className="rounded-md border border-warning/40 bg-warning/10 p-2 text-sm">
                <p className="flex items-center gap-1 text-warning">
                  <AlertTriangle className="h-4 w-4" /> {d.label} ({d.device}) {t("usb.notConfigured")}
                </p>
                <p className="text-xs text-muted-foreground">
                  {d.filesystem ?? "?"} · {t("usb.freeSpace", { free: d.freeBytes != null ? formatBytes(d.freeBytes) : "?" })}
                  {d.totalBytes != null ? t("usb.freeSpaceTotal", { total: formatBytes(d.totalBytes) }) : ""}
                </p>
                <Button
                  size="sm"
                  variant="outline"
                  className="mt-2"
                  disabled={enabling === d.mountpoint}
                  onClick={() => enableDrive(d.mountpoint)}
                >
                  {enabling === d.mountpoint ? t("usb.activating") : t("usb.activate")}
                </Button>
              </div>
            )
          )}

          {status.drives.some((d) => d.isBackupConfigured) && (
            <Button size="sm" variant="outline" onClick={loadArchives}>
              {showArchives ? t("usb.hideArchives") : t("usb.browseArchives")}
            </Button>
          )}

          {showArchives && (
            <div className="flex flex-col gap-1">
              {loadingArchives && <p className="text-xs text-muted-foreground">{t("usb.loading")}</p>}
              {!loadingArchives && archives?.length === 0 && (
                <p className="text-xs text-muted-foreground">{t("usb.noArchivesFound")}</p>
              )}
              {archives?.map((a) => (
                <div key={a.fullPath} className="flex items-center justify-between text-xs">
                  <div className="min-w-0">
                    <p className="truncate">
                      {a.category}/{a.sourceRef}/{a.fileName}
                    </p>
                    <p className="text-muted-foreground">
                      {formatBytes(a.sizeBytes)} · {new Date(a.modifiedAt).toLocaleString()}
                    </p>
                  </div>
                </div>
              ))}
            </div>
          )}
        </div>
      )}
    </Card>
  );
}

/** Capture-only card: takes a whole-server "migration" snapshot onto the
 * configured USB drive (disaster recovery / hardware swap). Restoring FROM a
 * snapshot is deliberately not here — that's Restore.tsx's job, same
 * separation as every other backup category in this app. */
function MigrationSnapshotCard() {
  const { t } = useTranslation("backups");
  const [usbConfigured, setUsbConfigured] = useState(false);
  // Décoché par défaut — un conteneur "-duplicate" est un clone de bascule
  // manuelle permanent (voir Sites > Dupliquer), identique à son original
  // au moment de la capture ; l'inclure double le temps/l'espace USB pour
  // un artefact qui n'a de sens que sur CE serveur, pas pour une
  // restauration sur une machine neuve.
  const [includeDuplicates, setIncludeDuplicates] = useState(false);
  const { snapshots, starting, activeManifestId, error, startWholeServerSnapshot } = useMigrationSnapshot();

  useEffect(() => {
    apiJson<UsbStatus>("/backups/usb/status")
      .then((s) => setUsbConfigured(s.drives.some((d) => d.isBackupConfigured)))
      .catch(() => setUsbConfigured(false));
  }, []);

  function startSnapshot() {
    startWholeServerSnapshot(includeDuplicates).catch(() => {});
  }

  return (
    <Card>
      <CardTitle className="flex items-center gap-1">
        <HardDriveUpload className="h-4 w-4" /> {t("migration.title")}
      </CardTitle>
      <p className="text-xs text-muted-foreground">{t("migration.explanation")}</p>

      {error && <p className="mt-2 text-xs text-destructive">{error}</p>}

      {!usbConfigured ? (
        <p className="mt-2 flex items-center gap-1 text-sm text-warning">
          <AlertTriangle className="h-4 w-4" /> {t("migration.noUsbConfigured")}
        </p>
      ) : (
        <>
          <label className="mt-2 flex items-center gap-2 text-xs text-muted-foreground">
            <input
              type="checkbox"
              checked={includeDuplicates}
              onChange={(e) => setIncludeDuplicates(e.target.checked)}
              disabled={starting || !!activeManifestId}
            />
            {t("migration.includeDuplicates")}
          </label>
          <Button size="sm" variant="outline" className="mt-2" onClick={startSnapshot} disabled={starting || !!activeManifestId}>
            {starting || activeManifestId ? (
              <>
                <Loader2 className="h-3.5 w-3.5 animate-spin" /> {t("migration.capturing")}
              </>
            ) : (
              t("migration.startSnapshot")
            )}
          </Button>
        </>
      )}

      {snapshots && snapshots.length > 0 && (
        <div className="mt-3 flex flex-col gap-1">
          {snapshots.map((s) => (
            <div key={s.manifestId} className="flex items-center justify-between text-xs">
              <span className="truncate text-muted-foreground">
                {s.scope.type === "site" ? t("migration.siteLabel", { name: s.scope.siteName }) : t("migration.fullServer")} ·{" "}
                {new Date(s.startedAt).toLocaleString()}
              </span>
              <span
                className={
                  "shrink-0 rounded-full px-2 py-0.5 font-medium " +
                  (s.status === "success"
                    ? "bg-primary/15 text-primary"
                    : s.status === "failed"
                      ? "bg-destructive/15 text-destructive"
                      : s.status === "partial"
                        ? "bg-warning/15 text-warning"
                        : "bg-muted text-muted-foreground")
                }
              >
                {s.status}
              </span>
            </div>
          ))}
        </div>
      )}
    </Card>
  );
}
```

- [ ] **Step 3: Vérifier le typecheck**

Run: `npx tsc --noEmit -p apps/web/tsconfig.json`
Expected: aucune erreur (hors bruit préexistant `ImaNote.tsx`).

- [ ] **Step 4: Vérifier la parité des clés des 3 fichiers `backups.json` complets**

Run:
```bash
node -e "
const fs = require('fs');
function flatten(obj, prefix = '') {
  let keys = [];
  for (const k in obj) {
    const path = prefix ? prefix + '.' + k : k;
    if (typeof obj[k] === 'object' && obj[k] !== null) {
      keys = keys.concat(flatten(obj[k], path));
    } else {
      keys.push(path);
    }
  }
  return keys.sort();
}
const langs = ['fr', 'en', 'ta'];
const keysets = {};
for (const lang of langs) {
  const content = JSON.parse(fs.readFileSync(\`apps/web/public/locales/\${lang}/backups.json\`, 'utf-8'));
  keysets[lang] = flatten(content).join(',');
}
console.log(keysets.fr === keysets.en && keysets.en === keysets.ta ? 'OK: key sets match' : 'MISMATCH');
"
```
Expected: `OK: key sets match`.

- [ ] **Step 5: Build de production et vérification**

Run (depuis la racine du repo, avec le contournement `ImaNote.tsx` si nécessaire) :
```bash
npm run build --workspace=packages/shared
npm run build:api
npm run build:web
```
Expected : les 3 builds réussissent. Vérifier que `apps/web/dist/locales/{fr,en,ta}/backups.json` existent et contiennent `gdrive`/`usb`/`migration`.

- [ ] **Step 6: Test manuel de régression**

Run: `npm run dev:web`, naviguer sur `/backups` en français puis en anglais et tamoul. Vérifier qu'aucune clé de traduction brute n'apparaît. Si un serveur dev est lancé, vérifier qu'il est bien arrêté avant de terminer.

- [ ] **Step 7: Commit**

```bash
git add apps/web/public/locales/fr/backups.json apps/web/public/locales/en/backups.json apps/web/public/locales/ta/backups.json apps/web/src/routes/Backups.tsx
git commit -m "feat(web): traduire les cartes Google Drive/USB/Migration de Backups via i18next"
```

---

## Fin de ce plan

Écran Backups entièrement traduit. Restent dans le lot "ops" : Restore, UsbExplorer, System, Services.
