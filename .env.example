# --- Serveur ---
NODE_ENV=production
PORT=8443
HOST=0.0.0.0
# TLS optionnel: si renseigné, le serveur écoute en HTTPS. Recommandé avec un
# certificat Tailscale (tailscale cert <machine>.<tailnet>.ts.net) pour un
# accès admin exclusivement via Tailscale, jamais exposé publiquement.
TLS_CERT_PATH=
TLS_KEY_PATH=

# --- Auth ---
# Générer avec: openssl rand -base64 48
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
TOTP_ISSUER=PiAdmin

# --- Base de données (métadonnées de l'app) ---
SQLITE_PATH=./data/app.db

# --- Docker ---
DOCKER_SOCKET_PATH=/var/run/docker.sock

# --- Reverse proxy ---
PROXY_TYPE=nginx
NGINX_SITES_AVAILABLE=/etc/nginx/sites-available
NGINX_SITES_ENABLED=/etc/nginx/sites-enabled
NGINX_CONF_D=/etc/nginx/conf.d
NGINX_LOG_DIR=/var/log/nginx
NGINX_BINARY_PATH=/usr/sbin/nginx
NGINX_CONFIG_BACKUP_DIR=./data/nginx-config-history
CERTBOT_LIVE_DIR=/etc/letsencrypt/live

# --- OS / apt ---
OS_MODULE_ENABLED=true
APT_ALLOW_INSTALL_REMOVE=false
APT_JOB_LOG_DIR=./data/apt-job-logs

# --- Réseau / Sécurité ---
NETWORK_MODULE_ENABLED=true
FAIL2BAN_ENABLED=true
FAIL2BAN_CLIENT_PATH=/usr/bin/fail2ban-client
FAIL2BAN_DEFAULT_JAIL=sshd
ANALYTICS_LOG_LINE_CAP=200000

# --- Sauvegardes locales ---
BACKUP_LOCAL_ROOT=/mnt/usb-backup/pwa-admin
BACKUP_RETENTION_DAYS=30
BACKUP_RETENTION_MIN_COPIES=3

# --- Sauvegardes Google Drive (OAuth2 sur Drive personnel) ---
GDRIVE_ENABLED=false
GDRIVE_OAUTH_CLIENT_ID=
GDRIVE_OAUTH_CLIENT_SECRET=
GDRIVE_OAUTH_TOKEN_PATH=./secrets/gdrive-oauth-token.json
GDRIVE_ROOT_FOLDER_ID=

# --- Rate limiting ---
LOGIN_RATE_LIMIT_MAX=5
LOGIN_RATE_LIMIT_WINDOW=1m

# --- Seuils d'alerte ---
ALERT_TEMP_WARN_C=70
ALERT_TEMP_CRIT_C=80
ALERT_DISK_WARN_PCT=80
ALERT_DISK_CRIT_PCT=90
